shadow$provide.module$node_modules$d3_geo_projection$src$newton = function(require, module, exports) {
  function solve(f, y, x) {
    var steps = 100;
    x = x === void 0 ? 0 : +x;
    y = +y;
    do {
      var delta = f(x);
      var f1 = f(x + require$_DOT__SLASH_math_DOT_js.epsilon);
      delta === f1 && (f1 = delta + require$_DOT__SLASH_math_DOT_js.epsilon);
      x -= delta = -1 * require$_DOT__SLASH_math_DOT_js.epsilon * (delta - y) / (delta - f1);
    } while (steps-- > 0 && (0,require$_DOT__SLASH_math_DOT_js.abs)(delta) > require$_DOT__SLASH_math_DOT_js.epsilon);
    return steps < 0 ? NaN : x;
  }
  function solve2d(f, MAX_ITERATIONS, eps) {
    MAX_ITERATIONS === void 0 && (MAX_ITERATIONS = 40);
    eps === void 0 && (eps = require$_DOT__SLASH_math_DOT_js.epsilon2);
    return function(x, y, a, b) {
      a = a === void 0 ? 0 : +a;
      b = b === void 0 ? 0 : +b;
      for (var i = 0; i < MAX_ITERATIONS; i++) {
        var p = f(a, b), tx = p[0] - x, ty = p[1] - y;
        if ((0,require$_DOT__SLASH_math_DOT_js.abs)(tx) < eps && (0,require$_DOT__SLASH_math_DOT_js.abs)(ty) < eps) {
          break;
        }
        var h = tx * tx + ty * ty;
        if (h > err2) {
          a -= da /= 2, b -= db /= 2;
        } else {
          var err2 = h;
          h = (a > 0 ? -1 : 1) * eps;
          var eb = (b > 0 ? -1 : 1) * eps;
          var da = f(a + h, b);
          var pb = f(a, b + eb);
          var db = (da[0] - p[0]) / h;
          h = (da[1] - p[1]) / h;
          da = (pb[0] - p[0]) / eb;
          p = (pb[1] - p[1]) / eb;
          eb = p * db - h * da;
          eb = ((0,require$_DOT__SLASH_math_DOT_js.abs)(eb) < 0.5 ? 0.5 : 1) / eb;
          da = (ty * da - tx * p) * eb;
          db = (tx * h - ty * db) * eb;
          a += da;
          b += db;
          if ((0,require$_DOT__SLASH_math_DOT_js.abs)(da) < eps && (0,require$_DOT__SLASH_math_DOT_js.abs)(db) < eps) {
            break;
          }
        }
      }
      return [a, b];
    };
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, solve:{enumerable:!0, get:function() {
    return solve;
  }}, solve2d:{enumerable:!0, get:function() {
    return solve2d;
  }}});
  var require$_DOT__SLASH_math_DOT_js = require("module$node_modules$d3_geo_projection$src$math");
};

//# sourceMappingURL=module$node_modules$d3_geo_projection$src$newton.js.map
