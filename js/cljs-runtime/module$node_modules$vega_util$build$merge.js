shadow$provide.module$node_modules$vega_util$build$merge = function(require, module, exports) {
  function merge(compare, array0, array1, output) {
    const n0 = array0.length, n1 = array1.length;
    if (!n1) {
      return array0;
    }
    if (!n0) {
      return array1;
    }
    var ctor = array0.constructor;
    output = output || (ctor === Array || typeof ctor === "function" && ctor.prototype && ArrayBuffer.isView(ctor.prototype) ? new ctor(n0 + n1) : Array(n0 + n1));
    let i1 = ctor = 0, i = 0;
    for (; ctor < n0 && i1 < n1; ++i) {
      output[i] = compare(array0[ctor], array1[i1]) > 0 ? array1[i1++] : array0[ctor++];
    }
    for (; ctor < n0; ++ctor, ++i) {
      output[i] = array0[ctor];
    }
    for (; i1 < n1; ++i1, ++i) {
      output[i] = array1[i1];
    }
    return output;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, default:{enumerable:!0, get:function() {
    return merge;
  }}});
};

//# sourceMappingURL=module$node_modules$vega_util$build$merge.js.map
