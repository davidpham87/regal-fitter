shadow$provide.module$node_modules$vega_runtime$build$vega_runtime = function(require, module, exports) {
  function expression(ctx, args, code) {
    code.endsWith(";") || (code = "return(" + code + ");");
    args = Function(...args.concat(code));
    return ctx && ctx.functions ? args.bind(ctx.functions) : args;
  }
  function parseParameter(spec, ctx, params) {
    if (!spec || !(0,require$vega_util.isObject)(spec)) {
      return spec;
    }
    for (let i = 0, n = PARSERS.length, p; i < n; ++i) {
      if (p = PARSERS[i], (0,require$vega_util.hasOwnProperty)(spec, p.key)) {
        return p.parse(spec, ctx, params);
      }
    }
    return spec;
  }
  function context(df, transforms, functions, expr) {
    return new Context(df, transforms, functions, expr);
  }
  function Context(df, transforms, functions, expr) {
    this.dataflow = df;
    this.transforms = transforms;
    this.events = df.events.bind(df);
    this.expr = expr || expressionCodegen;
    this.signals = {};
    this.scales = {};
    this.nodes = {};
    this.data = {};
    this.fn = {};
    functions && (this.functions = Object.create(functions), this.functions.context = this);
  }
  function Subcontext(ctx) {
    this.dataflow = ctx.dataflow;
    this.transforms = ctx.transforms;
    this.events = ctx.events;
    this.expr = ctx.expr;
    this.signals = Object.create(ctx.signals);
    this.scales = Object.create(ctx.scales);
    this.nodes = Object.create(ctx.nodes);
    this.data = Object.create(ctx.data);
    this.fn = Object.create(ctx.fn);
    ctx.functions && (this.functions = Object.create(ctx.functions), this.functions.context = this);
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, context:{enumerable:!0, get:function() {
    return context;
  }}});
  var require$vega_util = require("module$node_modules$vega_util$build$index"), require$vega_dataflow = require("module$node_modules$vega_dataflow$build$vega_dataflow");
  const Skip = (0,require$vega_util.toSet)(["rule"]), Swap = (0,require$vega_util.toSet)(["group", "image", "rect"]);
  var expressionCodegen = {operator:(ctx, expr) => expression(ctx, ["_"], expr.code), parameter:(ctx, expr) => expression(ctx, ["datum", "_"], expr.code), event:(ctx, expr) => expression(ctx, ["event"], expr.code), handler:(ctx, expr) => expression(ctx, ["_", "event"], `var datum=event.item&&event.item.datum;return ${expr.code};`), encode:(ctx, encode) => {
    const {marktype, channels} = encode;
    encode = "var o\x3ditem,datum\x3do.datum,m\x3d0,$;";
    for (var name in channels) {
      const o = "o[" + (0,require$vega_util.stringValue)(name) + "]";
      encode += `$=${channels[name].code};if(${o}!==$)${o}=$,m=1;`;
    }
    name = "";
    Skip[marktype] || (channels.x2 && (channels.x ? (Swap[marktype] && (name += "if(o.x\x3eo.x2)$\x3do.x,o.x\x3do.x2,o.x2\x3d$;"), name += "o.width\x3do.x2-o.x;") : name += "o.x\x3do.x2-(o.width||0);"), channels.xc && (name += "o.x\x3do.xc-(o.width||0)/2;"), channels.y2 && (channels.y ? (Swap[marktype] && (name += "if(o.y\x3eo.y2)$\x3do.y,o.y\x3do.y2,o.y2\x3d$;"), name += "o.height\x3do.y2-o.y;") : name += "o.y\x3do.y2-(o.height||0);"), channels.yc && (name += "o.y\x3do.yc-(o.height||0)/2;"));
    encode += name;
    return expression(ctx, ["item", "_"], encode + "return m;");
  }, codegen:{get(path) {
    path = `[${path.map(require$vega_util.stringValue).join("][")}]`;
    const get = Function("_", `return _${path};`);
    get.path = path;
    return get;
  }, comparator(fields, orders) {
    let t;
    fields = Function("a", "b", "var u, v; return " + fields.map((f, i) => {
      const o = orders[i];
      let u;
      f.path ? (u = `a${f.path}`, f = `b${f.path}`) : ((t = t || {})["f" + i] = f, u = `this.f${i}(a)`, f = `this.f${i}(b)`);
      i = -o;
      return `((u = ${u}) < (v = ${f}) || u == null) && v != null ? ${i}
  : (u > v || v == null) && u != null ? ${o}
  : ((v = v instanceof Date ? +v : v), (u = u instanceof Date ? +u : u)) !== u && v === v ? ${i}
  : v !== v && u === u ? ${o} : `;
    }).join("") + "0;");
    return t ? fields.bind(t) : fields;
  }}}, PARSERS = [{key:"$ref", parse:function(_, ctx) {
    return ctx.get(_.$ref) || (0,require$vega_util.error)("Operator not defined: " + _.$ref);
  }}, {key:"$key", parse:function(_, ctx) {
    const k = "k:" + _.$key + "_" + !!_.$flat;
    return ctx.fn[k] || (ctx.fn[k] = (0,require$vega_util.key)(_.$key, _.$flat, ctx.expr.codegen));
  }}, {key:"$expr", parse:function(_, ctx, params) {
    _.$params && ctx.parseParameters(_.$params, params);
    params = "e:" + _.$expr.code;
    return ctx.fn[params] || (ctx.fn[params] = (0,require$vega_util.accessor)(ctx.parameterExpression(_.$expr), _.$fields));
  }}, {key:"$field", parse:function(_, ctx) {
    if (!_.$field) {
      return null;
    }
    const k = "f:" + _.$field + "_" + _.$name;
    return ctx.fn[k] || (ctx.fn[k] = (0,require$vega_util.field)(_.$field, _.$name, ctx.expr.codegen));
  }}, {key:"$encode", parse:function(_, ctx) {
    _ = _.$encode;
    const encode = {};
    for (const name in _) {
      const enc = _[name];
      encode[name] = (0,require$vega_util.accessor)(ctx.encodeExpression(enc.$expr), enc.$fields);
      encode[name].output = enc.$output;
    }
    return encode;
  }}, {key:"$compare", parse:function(_, ctx) {
    const k = "c:" + _.$compare + "_" + _.$order, c = (0,require$vega_util.array)(_.$compare).map(_ => _ && _.$tupleid ? require$vega_dataflow.tupleid : _);
    return ctx.fn[k] || (ctx.fn[k] = (0,require$vega_util.compare)(c, _.$order, ctx.expr.codegen));
  }}, {key:"$context", parse:function(_, ctx) {
    return ctx;
  }}, {key:"$subflow", parse:function(_, ctx) {
    const spec = _.$subflow;
    return function(dataflow, key, parent) {
      const subctx = ctx.fork().parse(spec);
      dataflow = subctx.get(spec.operators[0].id);
      (key = subctx.signals.parent) && key.set(parent);
      dataflow.detachSubflow = () => ctx.detach(subctx);
      return dataflow;
    };
  }}, {key:"$tupleid", parse:function() {
    return require$vega_dataflow.tupleid;
  }}];
  const SKIP = {skip:!0};
  Context.prototype = Subcontext.prototype = {fork() {
    const ctx = new Subcontext(this);
    (this.subcontext || (this.subcontext = [])).push(ctx);
    return ctx;
  }, detach(ctx) {
    this.subcontext = this.subcontext.filter(c => c !== ctx);
    const keys = Object.keys(ctx.nodes);
    for (const key of keys) {
      ctx.nodes[key]._targets = null;
    }
    for (const key of keys) {
      ctx.nodes[key].detach();
    }
    ctx.nodes = null;
  }, get(id) {
    return this.nodes[id];
  }, set(id, node) {
    return this.nodes[id] = node;
  }, add(spec, op) {
    const ctx = this, df = ctx.dataflow, data = spec.value;
    ctx.set(spec.id, op);
    (spec.type + "").toLowerCase() === "collect" && data && (data.$ingest ? df.ingest(op, data.$ingest, data.$format) : data.$request ? df.preload(op, data.$request, data.$format) : df.pulse(op, df.changeset().insert(data)));
    spec.root && (ctx.root = op);
    if (spec.parent) {
      let p = ctx.get(spec.parent.$ref);
      p ? (df.connect(p, [op]), op.targets().add(p)) : (ctx.unresolved = ctx.unresolved || []).push(() => {
        p = ctx.get(spec.parent.$ref);
        df.connect(p, [op]);
        op.targets().add(p);
      });
    }
    spec.signal && (ctx.signals[spec.signal] = op);
    spec.scale && (ctx.scales[spec.scale] = op);
    if (spec.data) {
      for (const name in spec.data) {
        const data = ctx.data[name] || (ctx.data[name] = {});
        spec.data[name].forEach(role => data[role] = op);
      }
    }
  }, resolve() {
    (this.unresolved || []).forEach(fn => fn());
    delete this.unresolved;
    return this;
  }, operator(spec, update) {
    this.add(spec, this.dataflow.add(spec.value, update));
  }, transform(spec, type) {
    this.add(spec, this.dataflow.add(this.transforms[(type + "").toLowerCase()]));
  }, stream(spec, stream) {
    this.set(spec.id, stream);
  }, update(spec, stream, target, update, params) {
    this.dataflow.on(stream, target, update, params, spec.options);
  }, operatorExpression(expr) {
    return this.expr.operator(this, expr);
  }, parameterExpression(expr) {
    return this.expr.parameter(this, expr);
  }, eventExpression(expr) {
    return this.expr.event(this, expr);
  }, handlerExpression(expr) {
    return this.expr.handler(this, expr);
  }, encodeExpression(encode) {
    return this.expr.encode(this, encode);
  }, parse:function(spec) {
    const ctx = this, operators = spec.operators || [];
    spec.background && (ctx.background = spec.background);
    spec.eventConfig && (ctx.eventConfig = spec.eventConfig);
    spec.locale && (ctx.locale = spec.locale);
    operators.forEach(entry => ctx.parseOperator(entry));
    operators.forEach(entry => ctx.parseOperatorParameters(entry));
    (spec.streams || []).forEach(entry => ctx.parseStream(entry));
    (spec.updates || []).forEach(entry => ctx.parseUpdate(entry));
    return ctx.resolve();
  }, parseOperator:function(spec) {
    (spec.type + "").toLowerCase() !== "operator" && spec.type ? this.transform(spec, spec.type) : this.operator(spec, spec.update ? this.operatorExpression(spec.update) : null);
  }, parseOperatorParameters:function(spec) {
    if (spec.params) {
      const op = this.get(spec.id);
      op || (0,require$vega_util.error)("Invalid operator id: " + spec.id);
      this.dataflow.connect(op, op.parameters(this.parseParameters(spec.params), spec.react, spec.initonly));
    }
  }, parseParameters:function(spec, params) {
    params = params || {};
    const ctx = this;
    for (const key in spec) {
      const value = spec[key];
      params[key] = (0,require$vega_util.isArray)(value) ? value.map(v => parseParameter(v, ctx, params)) : parseParameter(value, ctx, params);
    }
    return params;
  }, parseStream:function(spec) {
    var ctx = this, filter = spec.filter != null ? ctx.eventExpression(spec.filter) : void 0, stream = spec.stream != null ? ctx.get(spec.stream) : void 0;
    if (spec.source) {
      stream = ctx.events(spec.source, spec.type, filter);
    } else if (spec.merge) {
      var args = spec.merge.map(_ => ctx.get(_));
      stream = args[0].merge.apply(args[0], args.slice(1));
    }
    spec.between && (args = spec.between.map(_ => ctx.get(_)), stream = stream.between(args[0], args[1]));
    spec.filter && (stream = stream.filter(filter));
    spec.throttle != null && (stream = stream.throttle(+spec.throttle));
    spec.debounce != null && (stream = stream.debounce(+spec.debounce));
    stream == null && (0,require$vega_util.error)("Invalid stream definition: " + JSON.stringify(spec));
    spec.consume && stream.consume(!0);
    ctx.stream(spec, stream);
  }, parseUpdate:function(spec) {
    var srcid = (0,require$vega_util.isObject)(srcid = spec.source) ? srcid.$ref : srcid;
    srcid = this.get(srcid);
    var update = spec.update, params = void 0;
    srcid || (0,require$vega_util.error)("Source not defined: " + spec.source);
    var target = spec.target && spec.target.$expr ? this.eventExpression(spec.target.$expr) : this.get(spec.target);
    update && update.$expr && (update.$params && (params = this.parseParameters(update.$params)), update = this.handlerExpression(update.$expr));
    this.update(spec, srcid, target, update, params);
  }, getState:function(options) {
    var ctx = this, state = {};
    if (options.signals) {
      var signals = state.signals = {};
      Object.keys(ctx.signals).forEach(key => {
        const op = ctx.signals[key];
        options.signals(key, op) && (signals[key] = op.value);
      });
    }
    if (options.data) {
      var data = state.data = {};
      Object.keys(ctx.data).forEach(key => {
        const dataset = ctx.data[key];
        options.data(key, dataset) && (data[key] = dataset.input.value);
      });
    }
    ctx.subcontext && options.recurse !== !1 && (state.subcontext = ctx.subcontext.map(ctx => ctx.getState(options)));
    return state;
  }, setState:function(state) {
    var ctx = this, df = ctx.dataflow, data = state.data, signals = state.signals;
    Object.keys(signals || {}).forEach(key => {
      df.update(ctx.signals[key], signals[key], SKIP);
    });
    Object.keys(data || {}).forEach(key => {
      df.pulse(ctx.data[key].input, df.changeset().remove(require$vega_util.truthy).insert(data[key]));
    });
    (state.subcontext || []).forEach((substate, i) => {
      (i = ctx.subcontext[i]) && i.setState(substate);
    });
  }};
};

//# sourceMappingURL=module$node_modules$vega_runtime$build$vega_runtime.js.map
