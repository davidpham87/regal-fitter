shadow$provide.module$node_modules$vega_transforms$build$vega_transforms = function(require, module, exports) {
  function multikey(f) {
    return x => {
      const n = f.length;
      let i = 1, k = String(f[0](x));
      for (; i < n; ++i) {
        k += "|" + f[i](x);
      }
      return k;
    };
  }
  function groupkey(fields) {
    return fields && fields.length ? fields.length === 1 ? fields[0] : multikey(fields) : function() {
      return "";
    };
  }
  function measure(key, value) {
    return (out, aggregate_param) => (0,require$vega_util.extend)({name:key, aggregate_param, out:out || key}, base_op, value);
  }
  function compareIndex(a, b) {
    return a.idx - b.idx;
  }
  function resolve(agg) {
    const map = {};
    agg.forEach(a => map[a.name] = a);
    const getreqs = a => {
      a.req && a.req.forEach(key => {
        map[key] || getreqs(map[key] = AggregateOps[key]());
      });
    };
    agg.forEach(getreqs);
    return Object.values(map).sort(compareIndex);
  }
  function init() {
    this.missing = this.valid = 0;
    this._ops.forEach(op => op.aggregate_param == null ? op.init(this) : op.init(this, op.aggregate_param));
  }
  function add(v, t) {
    v == null || v === "" ? ++this.missing : v === v && (++this.valid, this._ops.forEach(op => op.add(this, v, t)));
  }
  function rem(v, t) {
    v == null || v === "" ? --this.missing : v === v && (--this.valid, this._ops.forEach(op => op.rem(this, v, t)));
  }
  function set(t) {
    this._out.forEach(op => t[op.out] = op.value(this));
    return t;
  }
  function compileMeasures(agg, field) {
    function ctr(cell) {
      this._ops = ops;
      this._out = out;
      this.cell = cell;
      this.init();
    }
    field = field || require$vega_util.identity;
    const ops = resolve(agg), out = agg.slice().sort(compareIndex);
    ctr.prototype.init = init;
    ctr.prototype.add = add;
    ctr.prototype.rem = rem;
    ctr.prototype.set = set;
    ctr.prototype.get = field;
    ctr.fields = agg.map(op => op.out);
    return ctr;
  }
  function TupleStore(key) {
    this._key = key ? (0,require$vega_util.field)(key) : require$vega_dataflow.tupleid;
    this.reset();
  }
  function Aggregate(params) {
    require$vega_dataflow.Transform.call(this, null, params);
    this._adds = [];
    this._mods = [];
    this._mlen = this._alen = 0;
    this._drop = !0;
    this._cross = !1;
    this._dims = [];
    this._dnames = [];
    this._measures = [];
    this._countOnly = !1;
    this._outputs = this._inputs = this._prev = this._counts = null;
  }
  function Bin(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function SortedList(idFunc, source, input) {
    let data = source || [], add = input || [], rem = {}, cnt = 0;
    return {add:t => add.push(t), remove:t => rem[idFunc(t)] = ++cnt, size:() => data.length, data:(compare, resort) => {
      cnt && (data = data.filter(t => !rem[idFunc(t)]), rem = {}, cnt = 0);
      resort && compare && data.sort(compare);
      add.length && (data = compare ? (0,require$vega_util.merge)(compare, data, add.sort(compare)) : data.concat(add), add = []);
      return data;
    }};
  }
  function Collect(params) {
    require$vega_dataflow.Transform.call(this, [], params);
  }
  function Compare(params) {
    require$vega_dataflow.Operator.call(this, null, update$5, params);
  }
  function update$5(_) {
    return this.value && !_.modified() ? this.value : (0,require$vega_util.compare)(_.fields, _.orders);
  }
  function CountPattern(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function Cross(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function parse(def, data) {
    var func = def["function"];
    (0,require$vega_util.hasOwnProperty)(Distributions, func) || (0,require$vega_util.error)("Unknown distribution function: " + func);
    func = Distributions[func]();
    for (const name in def) {
      if (name === "field") {
        func.data((def.from || data()).map(def[name]));
      } else if (name === "distributions") {
        func[name](def[name].map(_ => parse(_, data)));
      } else if (typeof func[name] === "function") {
        func[name](def[name]);
      }
    }
    return func;
  }
  function Density(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function source(pulse) {
    return () => pulse.materialize(pulse.SOURCE).source;
  }
  function fieldNames(fields, as) {
    return fields ? fields.map((f, i) => as[i] || (0,require$vega_util.accessorName)(f)) : null;
  }
  function partition$1(data, groupby, field) {
    const groups = [], get = f => f(t);
    let map, i, n, t, k, g;
    if (groupby == null) {
      groups.push(data.map(field));
    } else {
      for (map = {}, i = 0, n = data.length; i < n; ++i) {
        t = data[i], k = groupby.map(get), g = map[k], g || (map[k] = g = [], g.dims = k, groups.push(g)), g.push(field(t));
      }
    }
    return groups;
  }
  function DotBin(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function Expression(params) {
    require$vega_dataflow.Operator.call(this, null, update$4, params);
    this.modified(!0);
  }
  function update$4(_) {
    const expr = _.expr;
    return this.value && !_.modified("expr") ? this.value : (0,require$vega_util.accessor)(datum => expr(datum, _), (0,require$vega_util.accessorFields)(expr), (0,require$vega_util.accessorName)(expr));
  }
  function Extent(params) {
    require$vega_dataflow.Transform.call(this, [void 0, void 0], params);
  }
  function Subflow(pulse, parent) {
    require$vega_dataflow.Operator.call(this, pulse);
    this.parent = parent;
    this.count = 0;
  }
  function Facet(params) {
    require$vega_dataflow.Transform.call(this, {}, params);
    this._keys = (0,require$vega_util.fastmap)();
    const a = this._targets = [];
    a.active = 0;
    a.forEach = f => {
      for (let i = 0, n = a.active; i < n; ++i) {
        f(a[i], i, a);
      }
    };
  }
  function Field(params) {
    require$vega_dataflow.Operator.call(this, null, update$3, params);
  }
  function update$3(_) {
    return this.value && !_.modified() ? this.value : (0,require$vega_util.isArray)(_.name) ? (0,require$vega_util.array)(_.name).map(f => (0,require$vega_util.field)(f)) : (0,require$vega_util.field)(_.name, _.as);
  }
  function Filter(params) {
    require$vega_dataflow.Transform.call(this, (0,require$vega_util.fastmap)(), params);
  }
  function Flatten(params) {
    require$vega_dataflow.Transform.call(this, [], params);
  }
  function Fold(params) {
    require$vega_dataflow.Transform.call(this, [], params);
  }
  function Formula(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function Generate(params) {
    require$vega_dataflow.Transform.call(this, [], params);
  }
  function Impute(params) {
    require$vega_dataflow.Transform.call(this, [], params);
  }
  function getValue(_) {
    var m = _.method || Methods.value;
    if (Methods[m] == null) {
      (0,require$vega_util.error)("Unrecognized imputation method: " + m);
    } else {
      if (m === Methods.value) {
        var v = _.value !== void 0 ? _.value : 0;
        return () => v;
      }
      return Methods[m];
    }
  }
  function getField(_) {
    const f = _.field;
    return t => t ? f(t) : NaN;
  }
  function partition(data, groupby, key, keyvals) {
    var get = f => f(t), groups = [];
    keyvals = keyvals ? keyvals.slice() : [];
    var kMap = {}, gMap = {}, group, n;
    keyvals.forEach((k, i) => kMap[k] = i + 1);
    var i = 0;
    for (n = data.length; i < n; ++i) {
      var t = data[i];
      var gVals = key(t);
      var j = kMap[gVals] || (kMap[gVals] = keyvals.push(gVals));
      var gKey = (gVals = groupby ? groupby.map(get) : Empty) + "";
      (group = gMap[gKey]) || (group = gMap[gKey] = [], groups.push(group), group.values = gVals);
      group[j - 1] = t;
    }
    groups.domain = keyvals;
    return groups;
  }
  function JoinAggregate(params) {
    Aggregate.call(this, params);
  }
  function KDE(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function Key(params) {
    require$vega_dataflow.Operator.call(this, null, update$2, params);
  }
  function update$2(_) {
    return this.value && !_.modified() ? this.value : (0,require$vega_util.key)(_.fields, _.flat);
  }
  function Load(params) {
    require$vega_dataflow.Transform.call(this, [], params);
    this._pending = null;
  }
  function stop(_) {
    return _.modified("async") && !(_.modified("values") || _.modified("url") || _.modified("format"));
  }
  function output(op, pulse, data) {
    data.forEach(require$vega_dataflow.ingest);
    pulse = pulse.fork(pulse.NO_FIELDS & pulse.NO_SOURCE);
    pulse.rem = op.value;
    op.value = pulse.source = pulse.add = data;
    op._pending = null;
    pulse.rem.length && pulse.clean(!0);
    return pulse;
  }
  function Lookup(params) {
    require$vega_dataflow.Transform.call(this, {}, params);
  }
  function MultiExtent(params) {
    require$vega_dataflow.Operator.call(this, null, update$1, params);
  }
  function update$1(_) {
    if (this.value && !_.modified()) {
      return this.value;
    }
    _ = _.extents;
    const n = _.length;
    let min = Infinity, max = -Infinity, i, e;
    for (i = 0; i < n; ++i) {
      e = _[i], e[0] < min && (min = e[0]), e[1] > max && (max = e[1]);
    }
    return [min, max];
  }
  function MultiValues(params) {
    require$vega_dataflow.Operator.call(this, null, update, params);
  }
  function update(_) {
    return this.value && !_.modified() ? this.value : _.values.reduce((data, _) => data.concat(_), []);
  }
  function Params(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function Pivot(params) {
    Aggregate.call(this, params);
  }
  function aggregateParams(_, pulse) {
    const key = _.field, value = _.value, op = (_.op === "count" ? "__count__" : _.op) || "sum", fields = (0,require$vega_util.accessorFields)(key).concat((0,require$vega_util.accessorFields)(value)), keys = pivotKeys(key, _.limit || 0, pulse);
    pulse.changed() && _.set("__pivot__", null, null, !0);
    return {key:_.key, groupby:_.groupby, ops:keys.map(() => op), fields:keys.map(k => get(k, key, value, fields)), as:keys.map(k => k + ""), modified:_.modified.bind(_)};
  }
  function get(k, key, value, fields) {
    return (0,require$vega_util.accessor)(d => key(d) === k ? value(d) : NaN, fields, k + "");
  }
  function pivotKeys(key, limit, pulse) {
    const map = {}, list = [];
    pulse.visit(pulse.SOURCE, t => {
      t = key(t);
      map[t] || (map[t] = 1, list.push(t));
    });
    list.sort(require$vega_util.ascending);
    return limit ? list.slice(0, limit) : list;
  }
  function PreFacet(params) {
    Facet.call(this, params);
  }
  function Project(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function Proxy(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function Quantile(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function Relay(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function Sample(params) {
    require$vega_dataflow.Transform.call(this, [], params);
    this.count = 0;
  }
  function Sequence(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function Sieve(params) {
    require$vega_dataflow.Transform.call(this, null, params);
    this.modified(!0);
  }
  function TimeUnit(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function TupleIndex(params) {
    require$vega_dataflow.Transform.call(this, (0,require$vega_util.fastmap)(), params);
  }
  function Values(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function WindowOp(op, field, param, as) {
    const fn = WindowOps[op](field, param);
    return {init:fn.init || require$vega_util.zero, update:function(w, t) {
      t[as] = fn.next(w);
    }};
  }
  function WindowState(_) {
    function visitInputs(f) {
      (0,require$vega_util.array)((0,require$vega_util.accessorFields)(f)).forEach(_ => inputs[_] = 1);
    }
    const ops = (0,require$vega_util.array)(_.ops), fields = (0,require$vega_util.array)(_.fields), params = (0,require$vega_util.array)(_.params), aggregate_params = (0,require$vega_util.array)(_.aggregate_params), as = (0,require$vega_util.array)(_.as), outputs = this.outputs = [], windows = this.windows = [], inputs = {}, map = {}, counts = [], measures = [];
    let countOnly = !0;
    visitInputs(_.sort);
    ops.forEach((op, i) => {
      const field = fields[i];
      var param = params[i];
      const aggregate_param = aggregate_params[i] || null, mname = (0,require$vega_util.accessorName)(field);
      i = as[i] || op + (mname ? "_" + mname : "");
      visitInputs(field);
      outputs.push(i);
      (0,require$vega_util.hasOwnProperty)(WindowOps, op) ? windows.push(WindowOp(op, field, param, i)) : (field == null && op !== "count" && (0,require$vega_util.error)("Null aggregate field specified."), op === "count" ? counts.push(i) : (countOnly = !1, param = map[mname], param || (param = map[mname] = [], param.field = field, measures.push(param)), param.push(AggregateOps[op](i, aggregate_param))));
    });
    if (counts.length || measures.length) {
      this.cell = cell(measures, counts, countOnly);
    }
    this.inputs = Object.keys(inputs);
  }
  function cell(measures, counts, countOnly) {
    measures = measures.map(m => compileMeasures(m, m.field));
    const cell = {num:0, agg:null, store:!1, count:counts};
    if (!countOnly) {
      for (var n = measures.length, a = cell.agg = Array(n), i = 0; i < n; ++i) {
        a[i] = new measures[i](cell);
      }
    }
    if (cell.store) {
      var store = cell.data = new TupleStore();
    }
    cell.add = function(t) {
      cell.num += 1;
      if (!countOnly) {
        store && store.add(t);
        for (let i = 0; i < n; ++i) {
          a[i].add(a[i].get(t), t);
        }
      }
    };
    cell.rem = function(t) {
      --cell.num;
      if (!countOnly) {
        store && store.rem(t);
        for (let i = 0; i < n; ++i) {
          a[i].rem(a[i].get(t), t);
        }
      }
    };
    cell.set = function(t) {
      let i, n;
      store && store.values();
      i = 0;
      for (n = counts.length; i < n; ++i) {
        t[counts[i]] = cell.num;
      }
      if (!countOnly) {
        for (i = 0, n = a.length; i < n; ++i) {
          a[i].set(t);
        }
      }
    };
    cell.init = function() {
      cell.num = 0;
      store && store.reset();
      for (let i = 0; i < n; ++i) {
        a[i].init();
      }
    };
    return cell;
  }
  function Window(params) {
    require$vega_dataflow.Transform.call(this, {}, params);
    this._mlen = 0;
    this._mods = [];
  }
  function processPartition(list, state, cmp, _) {
    var sort = _.sort;
    const range = sort && !_.ignorePeers;
    _ = _.frame || [null, 0];
    list = list.data(cmp);
    cmp = list.length;
    const b = range ? (0,require$d3_array.bisector)(sort) : null;
    sort = {i0:0, i1:0, p0:0, p1:0, index:0, data:list, compare:sort || (0,require$vega_util.constant)(-1)};
    state.init();
    for (let i = 0; i < cmp; ++i) {
      var w = sort, f = _, i$jscomp$0 = i;
      w.p0 = w.i0;
      w.p1 = w.i1;
      w.i0 = f[0] == null ? 0 : Math.max(0, i$jscomp$0 - Math.abs(f[0]));
      w.i1 = f[1] == null ? cmp : Math.min(cmp, i$jscomp$0 + Math.abs(f[1]) + 1);
      w.index = i$jscomp$0;
      if (range) {
        w = sort.i0;
        f = sort.i1 - 1;
        i$jscomp$0 = sort.compare;
        const d = sort.data, n = d.length - 1;
        w > 0 && !i$jscomp$0(d[w], d[w - 1]) && (sort.i0 = b.left(d, d[w]));
        f < n && !i$jscomp$0(d[f], d[f + 1]) && (sort.i1 = b.right(d, d[f]));
      }
      state.update(sort, list[i]);
    }
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, aggregate:{enumerable:!0, get:function() {
    return Aggregate;
  }}, bin:{enumerable:!0, get:function() {
    return Bin;
  }}, collect:{enumerable:!0, get:function() {
    return Collect;
  }}, compare:{enumerable:!0, get:function() {
    return Compare;
  }}, countpattern:{enumerable:!0, get:function() {
    return CountPattern;
  }}, cross:{enumerable:!0, get:function() {
    return Cross;
  }}, density:{enumerable:!0, get:function() {
    return Density;
  }}, dotbin:{enumerable:!0, get:function() {
    return DotBin;
  }}, expression:{enumerable:!0, get:function() {
    return Expression;
  }}, extent:{enumerable:!0, get:function() {
    return Extent;
  }}, facet:{enumerable:!0, get:function() {
    return Facet;
  }}, field:{enumerable:!0, get:function() {
    return Field;
  }}, filter:{enumerable:!0, get:function() {
    return Filter;
  }}, flatten:{enumerable:!0, get:function() {
    return Flatten;
  }}, fold:{enumerable:!0, get:function() {
    return Fold;
  }}, formula:{enumerable:!0, get:function() {
    return Formula;
  }}, generate:{enumerable:!0, get:function() {
    return Generate;
  }}, impute:{enumerable:!0, get:function() {
    return Impute;
  }}, joinaggregate:{enumerable:!0, get:function() {
    return JoinAggregate;
  }}, kde:{enumerable:!0, get:function() {
    return KDE;
  }}, key:{enumerable:!0, get:function() {
    return Key;
  }}, load:{enumerable:!0, get:function() {
    return Load;
  }}, lookup:{enumerable:!0, get:function() {
    return Lookup;
  }}, multiextent:{enumerable:!0, get:function() {
    return MultiExtent;
  }}, multivalues:{enumerable:!0, get:function() {
    return MultiValues;
  }}, params:{enumerable:!0, get:function() {
    return Params;
  }}, pivot:{enumerable:!0, get:function() {
    return Pivot;
  }}, prefacet:{enumerable:!0, get:function() {
    return PreFacet;
  }}, project:{enumerable:!0, get:function() {
    return Project;
  }}, proxy:{enumerable:!0, get:function() {
    return Proxy;
  }}, quantile:{enumerable:!0, get:function() {
    return Quantile;
  }}, relay:{enumerable:!0, get:function() {
    return Relay;
  }}, sample:{enumerable:!0, get:function() {
    return Sample;
  }}, sequence:{enumerable:!0, get:function() {
    return Sequence;
  }}, sieve:{enumerable:!0, get:function() {
    return Sieve;
  }}, subflow:{enumerable:!0, get:function() {
    return Subflow;
  }}, timeunit:{enumerable:!0, get:function() {
    return TimeUnit;
  }}, tupleindex:{enumerable:!0, get:function() {
    return TupleIndex;
  }}, values:{enumerable:!0, get:function() {
    return Values;
  }}, window:{enumerable:!0, get:function() {
    return Window;
  }}});
  var require$vega_util = require("module$node_modules$vega_util$build$index"), require$vega_dataflow = require("module$node_modules$vega_dataflow$build$vega_dataflow"), require$vega_statistics = require("module$node_modules$vega_statistics$build$vega_statistics"), require$d3_array = require("module$node_modules$d3_array$src$index"), require$vega_time = require("module$node_modules$vega_time$build$vega_time");
  require = () => {
  };
  const base_op = {init:require, add:require, rem:require, idx:0}, AggregateOps = {values:{init:m => m.cell.store = !0, value:m => m.cell.data.values(), idx:-1}, count:{value:m => m.cell.num}, __count__:{value:m => m.missing + m.valid}, missing:{value:m => m.missing}, valid:{value:m => m.valid}, sum:{init:m => m.sum = 0, value:m => m.valid ? m.sum : void 0, add:(m, v) => m.sum += +v, rem:(m, v) => m.sum -= v}, product:{init:m => m.product = 1, value:m => m.valid ? m.product : void 0, add:(m, v) => 
  m.product *= v, rem:(m, v) => m.product /= v}, mean:{init:m => m.mean = 0, value:m => m.valid ? m.mean : void 0, add:(m, v) => (m.mean_d = v - m.mean, m.mean += m.mean_d / m.valid), rem:(m, v) => (m.mean_d = v - m.mean, m.mean -= m.valid ? m.mean_d / m.valid : m.mean)}, average:{value:m => m.valid ? m.mean : void 0, req:["mean"], idx:1}, variance:{init:m => m.dev = 0, value:m => m.valid > 1 ? m.dev / (m.valid - 1) : void 0, add:(m, v) => m.dev += m.mean_d * (v - m.mean), rem:(m, v) => m.dev -= 
  m.mean_d * (v - m.mean), req:["mean"], idx:1}, variancep:{value:m => m.valid > 1 ? m.dev / m.valid : void 0, req:["variance"], idx:2}, stdev:{value:m => m.valid > 1 ? Math.sqrt(m.dev / (m.valid - 1)) : void 0, req:["variance"], idx:2}, stdevp:{value:m => m.valid > 1 ? Math.sqrt(m.dev / m.valid) : void 0, req:["variance"], idx:2}, stderr:{value:m => m.valid > 1 ? Math.sqrt(m.dev / (m.valid * (m.valid - 1))) : void 0, req:["variance"], idx:2}, distinct:{value:m => m.cell.data.distinct(m.get), req:["values"], 
  idx:3}, ci0:{value:m => m.cell.data.ci0(m.get), req:["values"], idx:3}, ci1:{value:m => m.cell.data.ci1(m.get), req:["values"], idx:3}, median:{value:m => m.cell.data.q2(m.get), req:["values"], idx:3}, q1:{value:m => m.cell.data.q1(m.get), req:["values"], idx:3}, q3:{value:m => m.cell.data.q3(m.get), req:["values"], idx:3}, min:{init:m => m.min = void 0, value:m => m.min = Number.isNaN(m.min) ? m.cell.data.min(m.get) : m.min, add:(m, v) => {
    if (v < m.min || m.min === void 0) {
      m.min = v;
    }
  }, rem:(m, v) => {
    v <= m.min && (m.min = NaN);
  }, req:["values"], idx:4}, max:{init:m => m.max = void 0, value:m => m.max = Number.isNaN(m.max) ? m.cell.data.max(m.get) : m.max, add:(m, v) => {
    if (v > m.max || m.max === void 0) {
      m.max = v;
    }
  }, rem:(m, v) => {
    v >= m.max && (m.max = NaN);
  }, req:["values"], idx:4}, argmin:{init:m => m.argmin = void 0, value:m => m.argmin || m.cell.data.argmin(m.get), add:(m, v, t) => {
    v < m.min && (m.argmin = t);
  }, rem:(m, v) => {
    v <= m.min && (m.argmin = void 0);
  }, req:["min", "values"], idx:3}, argmax:{init:m => m.argmax = void 0, value:m => m.argmax || m.cell.data.argmax(m.get), add:(m, v, t) => {
    v > m.max && (m.argmax = t);
  }, rem:(m, v) => {
    v >= m.max && (m.argmax = void 0);
  }, req:["max", "values"], idx:3}, exponential:{init:(m, r) => {
    m.exp = 0;
    m.exp_r = r;
  }, value:m => m.valid ? m.exp * (1 - m.exp_r) / (1 - m.exp_r ** m.valid) : void 0, add:(m, v) => m.exp = m.exp_r * m.exp + v, rem:(m, v) => m.exp = (m.exp - v / m.exp_r ** (m.valid - 1)) / m.exp_r}, exponentialb:{value:m => m.valid ? m.exp * (1 - m.exp_r) : void 0, req:["exponential"], idx:1}};
  require = Object.keys(AggregateOps).filter(d => d !== "__count__");
  [...require, "__count__"].forEach(key => {
    AggregateOps[key] = measure(key, AggregateOps[key]);
  });
  module = TupleStore.prototype;
  module.reset = function() {
    this._add = [];
    this._rem = [];
    this._q = this._get = this._ext = null;
  };
  module.add = function(v) {
    this._add.push(v);
  };
  module.rem = function(v) {
    this._rem.push(v);
  };
  module.values = function() {
    this._get = null;
    if (this._rem.length === 0) {
      return this._add;
    }
    const a = this._add;
    var r = this._rem;
    const k = this._key, n = a.length, m = r.length, x = Array(n - m), map = {};
    let i, v;
    for (i = 0; i < m; ++i) {
      map[k(r[i])] = 1;
    }
    for (r = i = 0; i < n; ++i) {
      map[k(v = a[i])] ? map[k(v)] = 0 : x[r++] = v;
    }
    this._rem = [];
    return this._add = x;
  };
  module.distinct = function(get) {
    const v = this.values(), map = {};
    let n = v.length, count = 0, s;
    for (; --n >= 0;) {
      s = get(v[n]) + "", (0,require$vega_util.hasOwnProperty)(map, s) || (map[s] = 1, ++count);
    }
    return count;
  };
  module.extent = function(get) {
    if (this._get !== get || !this._ext) {
      const v = this.values(), i = (0,require$vega_util.extentIndex)(v, get);
      this._ext = [v[i[0]], v[i[1]]];
      this._get = get;
    }
    return this._ext;
  };
  module.argmin = function(get) {
    return this.extent(get)[0] || {};
  };
  module.argmax = function(get) {
    return this.extent(get)[1] || {};
  };
  module.min = function(get) {
    const m = this.extent(get)[0];
    return m != null ? get(m) : void 0;
  };
  module.max = function(get) {
    const m = this.extent(get)[1];
    return m != null ? get(m) : void 0;
  };
  module.quartile = function(get) {
    this._get === get && this._q || (this._q = (0,require$vega_statistics.quartiles)(this.values(), get), this._get = get);
    return this._q;
  };
  module.q1 = function(get) {
    return this.quartile(get)[0];
  };
  module.q2 = function(get) {
    return this.quartile(get)[1];
  };
  module.q3 = function(get) {
    return this.quartile(get)[2];
  };
  module.ci = function(get) {
    this._get === get && this._ci || (this._ci = (0,require$vega_statistics.bootstrapCI)(this.values(), 1000, 0.05, get), this._get = get);
    return this._ci;
  };
  module.ci0 = function(get) {
    return this.ci(get)[0];
  };
  module.ci1 = function(get) {
    return this.ci(get)[1];
  };
  Aggregate.Definition = {type:"Aggregate", metadata:{generates:!0, changes:!0}, params:[{name:"groupby", type:"field", array:!0}, {name:"ops", type:"enum", array:!0, values:require}, {name:"aggregate_params", type:"number", "null":!0, array:!0}, {name:"fields", type:"field", "null":!0, array:!0}, {name:"as", type:"string", "null":!0, array:!0}, {name:"drop", type:"boolean", "default":!0}, {name:"cross", type:"boolean", "default":!1}, {name:"key", type:"field"}]};
  (0,require$vega_util.inherits)(Aggregate, require$vega_dataflow.Transform, {transform(_, pulse) {
    const aggr = this, out = pulse.fork(pulse.NO_SOURCE | pulse.NO_FIELDS), mod = _.modified();
    aggr.stamp = out.stamp;
    aggr.value && (mod || pulse.modified(aggr._inputs, !0)) ? (aggr._prev = aggr.value, aggr.value = mod ? aggr.init(_) : Object.create(null), pulse.visit(pulse.SOURCE, t => aggr.add(t))) : (aggr.value = aggr.value || aggr.init(_), pulse.visit(pulse.REM, t => aggr.rem(t)), pulse.visit(pulse.ADD, t => aggr.add(t)));
    out.modifies(aggr._outputs);
    aggr._drop = _.drop !== !1;
    _.cross && aggr._dims.length > 1 && (aggr._drop = !1, aggr.cross());
    pulse.clean() && aggr._drop && out.clean(!0).runAfter(() => this.clean());
    return aggr.changes(out);
  }, cross() {
    function collect(cells) {
      let key, i, t, v;
      for (key in cells) {
        for (t = cells[key].tuple, i = 0; i < n; ++i) {
          vals[i][v = t[dims[i]]] = v;
        }
      }
    }
    function generate(base, tuple, index) {
      const name = dims[index], v = vals[index++];
      for (const k in v) {
        const key = base ? base + "|" + k : k;
        tuple[name] = v[k];
        index < n ? generate(key, tuple, index) : curr[key] || aggr.cell(key, tuple);
      }
    }
    const aggr = this, curr = aggr.value, dims = aggr._dnames, vals = dims.map(() => ({})), n = dims.length;
    collect(aggr._prev);
    collect(curr);
    generate("", {}, 0);
  }, init(_) {
    function inputVisit(get) {
      get = (0,require$vega_util.array)((0,require$vega_util.accessorFields)(get));
      const n = get.length;
      let i = 0, f;
      for (; i < n; ++i) {
        inputMap[f = get[i]] || (inputMap[f] = 1, inputs.push(f));
      }
    }
    const inputs = this._inputs = [], outputs = this._outputs = [], inputMap = {};
    this._dims = (0,require$vega_util.array)(_.groupby);
    this._dnames = this._dims.map(d => {
      const dname = (0,require$vega_util.accessorName)(d);
      inputVisit(d);
      outputs.push(dname);
      return dname;
    });
    this.cellkey = _.key ? _.key : groupkey(this._dims);
    this._countOnly = !0;
    this._counts = [];
    this._measures = [];
    const fields = _.fields || [null], ops = _.ops || ["count"], aggregate_params = _.aggregate_params || [null];
    _ = _.as || [];
    const n = fields.length, map = {};
    let field, op, aggregate_param, m, mname, outname, i;
    n !== ops.length && (0,require$vega_util.error)("Unmatched number of fields and aggregate ops.");
    for (i = 0; i < n; ++i) {
      field = fields[i], op = ops[i], aggregate_param = aggregate_params[i] || null, field == null && op !== "count" && (0,require$vega_util.error)("Null aggregate field specified."), mname = (0,require$vega_util.accessorName)(field), outname = _[i] || op + (mname ? "_" + mname : ""), outputs.push(outname), op === "count" ? this._counts.push(outname) : (m = map[mname], m || (inputVisit(field), m = map[mname] = [], m.field = field, this._measures.push(m)), op !== "count" && (this._countOnly = !1), 
      m.push(AggregateOps[op](outname, aggregate_param)));
    }
    this._measures = this._measures.map(m => compileMeasures(m, m.field));
    return Object.create(null);
  }, cellkey:groupkey(), cell(key, t) {
    let cell = this.value[key];
    cell ? cell.num === 0 && this._drop && cell.stamp < this.stamp ? (cell.stamp = this.stamp, this._adds[this._alen++] = cell) : cell.stamp < this.stamp && (cell.stamp = this.stamp, this._mods[this._mlen++] = cell) : (cell = this.value[key] = this.newcell(key, t), this._adds[this._alen++] = cell);
    return cell;
  }, newcell(key, t) {
    key = {key, num:0, agg:null, tuple:this.newtuple(t, this._prev && this._prev[key]), stamp:this.stamp, store:!1};
    if (!this._countOnly) {
      t = this._measures;
      const n = t.length;
      key.agg = Array(n);
      for (let i = 0; i < n; ++i) {
        key.agg[i] = new t[i](key);
      }
    }
    key.store && (key.data = new TupleStore());
    return key;
  }, newtuple(t, p) {
    const names = this._dnames, dims = this._dims, n = dims.length, x = {};
    for (let i = 0; i < n; ++i) {
      x[names[i]] = dims[i](t);
    }
    return p ? (0,require$vega_dataflow.replace)(p.tuple, x) : (0,require$vega_dataflow.ingest)(x);
  }, clean() {
    const cells = this.value;
    for (const key in cells) {
      cells[key].num === 0 && delete cells[key];
    }
  }, add(t) {
    var key = this.cellkey(t);
    key = this.cell(key, t);
    key.num += 1;
    if (!this._countOnly) {
      key.store && key.data.add(t);
      key = key.agg;
      for (let i = 0, n = key.length; i < n; ++i) {
        key[i].add(key[i].get(t), t);
      }
    }
  }, rem(t) {
    var key = this.cellkey(t);
    key = this.cell(key, t);
    --key.num;
    if (!this._countOnly) {
      key.store && key.data.rem(t);
      key = key.agg;
      for (let i = 0, n = key.length; i < n; ++i) {
        key[i].rem(key[i].get(t), t);
      }
    }
  }, celltuple(cell) {
    const tuple = cell.tuple, counts = this._counts;
    cell.store && cell.data.values();
    for (let i = 0, n = counts.length; i < n; ++i) {
      tuple[counts[i]] = cell.num;
    }
    if (!this._countOnly) {
      cell = cell.agg;
      for (let i = 0, n = cell.length; i < n; ++i) {
        cell[i].set(tuple);
      }
    }
    return tuple;
  }, changes(out) {
    const adds = this._adds, mods = this._mods;
    var prev = this._prev;
    const drop = this._drop, add = out.add, rem = out.rem, mod = out.mod;
    let cell;
    var key;
    if (prev) {
      for (key in prev) {
        cell = prev[key], drop && !cell.num || rem.push(cell.tuple);
      }
    }
    prev = 0;
    for (key = this._alen; prev < key; ++prev) {
      add.push(this.celltuple(adds[prev])), adds[prev] = null;
    }
    prev = 0;
    for (key = this._mlen; prev < key; ++prev) {
      cell = mods[prev], (cell.num === 0 && drop ? rem : mod).push(this.celltuple(cell)), mods[prev] = null;
    }
    this._alen = this._mlen = 0;
    this._prev = null;
    return out;
  }});
  Bin.Definition = {type:"Bin", metadata:{modifies:!0}, params:[{name:"field", type:"field", required:!0}, {name:"interval", type:"boolean", "default":!0}, {name:"anchor", type:"number"}, {name:"maxbins", type:"number", "default":20}, {name:"base", type:"number", "default":10}, {name:"divide", type:"number", array:!0, "default":[5, 2]}, {name:"extent", type:"number", array:!0, length:2, required:!0}, {name:"span", type:"number"}, {name:"step", type:"number"}, {name:"steps", type:"number", array:!0}, 
  {name:"minstep", type:"number", "default":0}, {name:"nice", type:"boolean", "default":!0}, {name:"name", type:"string"}, {name:"as", type:"string", array:!0, length:2, "default":["bin0", "bin1"]}]};
  (0,require$vega_util.inherits)(Bin, require$vega_dataflow.Transform, {transform(_, pulse) {
    const band = _.interval !== !1, bins = this._bins(_), start = bins.start, step = bins.step, as = _.as || ["bin0", "bin1"], b0 = as[0], b1 = as[1];
    _.modified() ? (pulse = pulse.reflow(!0), _ = pulse.SOURCE) : _ = pulse.modified((0,require$vega_util.accessorFields)(_.field)) ? pulse.ADD_MOD : pulse.ADD;
    pulse.visit(_, band ? t => {
      const v = bins(t);
      t[b0] = v;
      t[b1] = v == null ? null : start + step * (1 + (v - start) / step);
    } : t => t[b0] = bins(t));
    return pulse.modifies(band ? as : b0);
  }, _bins(_) {
    if (this.value && !_.modified()) {
      return this.value;
    }
    const field = _.field, bins = (0,require$vega_statistics.bin)(_), step = bins.step;
    let start = bins.start, stop = start + Math.ceil((bins.stop - start) / step) * step;
    var a;
    (a = _.anchor) != null && (a -= start + step * Math.floor((a - start) / step), start += a, stop += a);
    a = function(t) {
      t = (0,require$vega_util.toNumber)(field(t));
      return t == null ? null : t < start ? -Infinity : t > stop ? Infinity : (t = Math.max(start, Math.min(t, stop - step)), start + step * Math.floor(1e-14 + (t - start) / step));
    };
    a.start = start;
    a.stop = bins.stop;
    a.step = step;
    return this.value = (0,require$vega_util.accessor)(a, (0,require$vega_util.accessorFields)(field), _.name || "bin_" + (0,require$vega_util.accessorName)(field));
  }});
  Collect.Definition = {type:"Collect", metadata:{source:!0}, params:[{name:"sort", type:"compare"}]};
  (0,require$vega_util.inherits)(Collect, require$vega_dataflow.Transform, {transform(_, pulse) {
    const out = pulse.fork(pulse.ALL), list = SortedList(require$vega_dataflow.tupleid, this.value, out.materialize(out.ADD).add), sort = _.sort;
    _ = pulse.changed() || sort && (_.modified("sort") || pulse.modified(sort.fields));
    out.visit(out.REM, list.remove);
    this.modified(_);
    this.value = out.source = list.data((0,require$vega_dataflow.stableCompare)(sort), _);
    pulse.source && pulse.source.root && (this.value.root = pulse.source.root);
    return out;
  }});
  (0,require$vega_util.inherits)(Compare, require$vega_dataflow.Operator);
  CountPattern.Definition = {type:"CountPattern", metadata:{generates:!0, changes:!0}, params:[{name:"field", type:"field", required:!0}, {name:"case", type:"enum", values:["upper", "lower", "mixed"], "default":"mixed"}, {name:"pattern", type:"string", "default":'[\\w"]+'}, {name:"stopwords", type:"string", "default":""}, {name:"as", type:"string", array:!0, length:2, "default":["text", "count"]}]};
  (0,require$vega_util.inherits)(CountPattern, require$vega_dataflow.Transform, {transform(_, pulse) {
    var process = update => tuple => {
      tuple = get(tuple);
      switch(_.case) {
        case "upper":
          tuple = tuple.toUpperCase();
          break;
        case "lower":
          tuple = tuple.toLowerCase();
      }
      tuple = tuple.match(match) || [];
      for (var t, i = 0, n = tuple.length; i < n; ++i) {
        stop.test(t = tuple[i]) || update(t);
      }
    };
    const init = this._parameterCheck(_, pulse), counts = this._counts, match = this._match, stop = this._stop, get = _.field, as = _.as || ["text", "count"], add = process(t => counts[t] = 1 + (counts[t] || 0));
    process = process(t => --counts[t]);
    init ? pulse.visit(pulse.SOURCE, add) : (pulse.visit(pulse.ADD, add), pulse.visit(pulse.REM, process));
    return this._finish(pulse, as);
  }, _parameterCheck(_, pulse) {
    let init = !1;
    if (_.modified("stopwords") || !this._stop) {
      this._stop = new RegExp("^" + (_.stopwords || "") + "$", "i"), init = !0;
    }
    if (_.modified("pattern") || !this._match) {
      this._match = new RegExp(_.pattern || "[\\w']+", "g"), init = !0;
    }
    if (_.modified("field") || pulse.modified(_.field.fields)) {
      init = !0;
    }
    init && (this._counts = {});
    return init;
  }, _finish(pulse, as) {
    const counts = this._counts, tuples = this._tuples || (this._tuples = {}), text = as[0], count = as[1];
    pulse = pulse.fork(pulse.NO_SOURCE | pulse.NO_FIELDS);
    let w, t, c;
    for (w in counts) {
      t = tuples[w], c = counts[w] || 0, !t && c ? (tuples[w] = t = (0,require$vega_dataflow.ingest)({}), t[text] = w, t[count] = c, pulse.add.push(t)) : c === 0 ? (t && pulse.rem.push(t), counts[w] = null, tuples[w] = null) : t[count] !== c && (t[count] = c, pulse.mod.push(t));
    }
    return pulse.modifies(as);
  }});
  Cross.Definition = {type:"Cross", metadata:{generates:!0}, params:[{name:"filter", type:"expr"}, {name:"as", type:"string", array:!0, length:2, "default":["a", "b"]}]};
  (0,require$vega_util.inherits)(Cross, require$vega_dataflow.Transform, {transform(_, pulse) {
    const out = pulse.fork(pulse.NO_SOURCE), as = _.as || ["a", "b"], a = as[0], b = as[1];
    var reset = !this.value || pulse.changed(pulse.ADD_REM) || _.modified("as") || _.modified("filter"), data = this.value;
    if (reset) {
      data && (out.rem = data);
      pulse = data = pulse.materialize(pulse.SOURCE).source;
      _ = _.filter || require$vega_util.truthy;
      reset = [];
      data = {};
      for (var n = pulse.length, i = 0, j, left; i < n; ++i) {
        for (data[a] = left = pulse[i], j = 0; j < n; ++j) {
          data[b] = pulse[j], _(data) && (reset.push((0,require$vega_dataflow.ingest)(data)), data = {}, data[a] = left);
        }
      }
      out.add = this.value = reset;
    } else {
      out.mod = data;
    }
    out.source = this.value;
    return out.modifies(as);
  }});
  const Distributions = {kde:require$vega_statistics.randomKDE, mixture:require$vega_statistics.randomMixture, normal:require$vega_statistics.randomNormal, lognormal:require$vega_statistics.randomLogNormal, uniform:require$vega_statistics.randomUniform};
  module = [{key:{"function":"normal"}, params:[{name:"mean", type:"number", "default":0}, {name:"stdev", type:"number", "default":1}]}, {key:{"function":"lognormal"}, params:[{name:"mean", type:"number", "default":0}, {name:"stdev", type:"number", "default":1}]}, {key:{"function":"uniform"}, params:[{name:"min", type:"number", "default":0}, {name:"max", type:"number", "default":1}]}, {key:{"function":"kde"}, params:[{name:"field", type:"field", required:!0}, {name:"from", type:"data"}, {name:"bandwidth", 
  type:"number", "default":0}]}];
  Density.Definition = {type:"Density", metadata:{generates:!0}, params:[{name:"extent", type:"number", array:!0, length:2}, {name:"steps", type:"number"}, {name:"minsteps", type:"number", "default":25}, {name:"maxsteps", type:"number", "default":200}, {name:"method", type:"string", "default":"pdf", values:["pdf", "cdf"]}, {name:"distribution", type:"param", params:module.concat({key:{"function":"mixture"}, params:[{name:"distributions", type:"param", array:!0, params:module}, {name:"weights", type:"number", 
  array:!0}]})}, {name:"as", type:"string", array:!0, "default":["value", "density"]}]};
  (0,require$vega_util.inherits)(Density, require$vega_dataflow.Transform, {transform(_, pulse) {
    const out = pulse.fork(pulse.NO_SOURCE | pulse.NO_FIELDS);
    if (!this.value || pulse.changed() || _.modified()) {
      const dist = parse(_.distribution, source(pulse));
      pulse = _.steps || _.minsteps || 25;
      const maxsteps = _.steps || _.maxsteps || 200;
      let method = _.method || "pdf";
      method !== "pdf" && method !== "cdf" && (0,require$vega_util.error)("Invalid density method: " + method);
      _.extent || dist.data || (0,require$vega_util.error)("Missing density extent parameter.");
      method = dist[method];
      const as = _.as || ["value", "density"];
      _ = _.extent || (0,require$vega_util.extent)(dist.data());
      _ = (0,require$vega_statistics.sampleCurve)(method, _, pulse, maxsteps).map(v => {
        const tuple = {};
        tuple[as[0]] = v[0];
        tuple[as[1]] = v[1];
        return (0,require$vega_dataflow.ingest)(tuple);
      });
      this.value && (out.rem = this.value);
      this.value = out.add = out.source = _;
    }
    return out;
  }});
  DotBin.Definition = {type:"DotBin", metadata:{modifies:!0}, params:[{name:"field", type:"field", required:!0}, {name:"groupby", type:"field", array:!0}, {name:"step", type:"number"}, {name:"smooth", type:"boolean", "default":!1}, {name:"as", type:"string", "default":"bin"}]};
  (0,require$vega_util.inherits)(DotBin, require$vega_dataflow.Transform, {transform(_, pulse) {
    if (this.value && !_.modified() && !pulse.changed()) {
      return pulse;
    }
    var source = pulse.materialize(pulse.SOURCE).source;
    const groups = partition$1(pulse.source, _.groupby, require$vega_util.identity), smooth = _.smooth || !1, field = _.field;
    source = _.step || (0,require$vega_util.span)((0,require$vega_util.extent)(source, field)) / 30;
    const sort = (0,require$vega_dataflow.stableCompare)((a, b) => field(a) - field(b));
    _ = _.as || "bin";
    const n = groups.length;
    let min = Infinity, max = -Infinity, i = 0, j;
    for (; i < n; ++i) {
      const g = groups[i].sort(sort);
      j = -1;
      for (const v of (0,require$vega_statistics.dotbin)(g, source, smooth, field)) {
        v < min && (min = v), v > max && (max = v), g[++j][_] = v;
      }
    }
    this.value = {start:min, stop:max, step:source};
    return pulse.reflow(!0).modifies(_);
  }});
  (0,require$vega_util.inherits)(Expression, require$vega_dataflow.Operator);
  Extent.Definition = {type:"Extent", metadata:{}, params:[{name:"field", type:"field", required:!0}]};
  (0,require$vega_util.inherits)(Extent, require$vega_dataflow.Transform, {transform(_, pulse) {
    var extent = this.value;
    const field = _.field;
    _ = pulse.changed() || pulse.modified(field.fields) || _.modified("field");
    let min = extent[0], max = extent[1];
    if (_ || min == null) {
      min = Infinity, max = -Infinity;
    }
    pulse.visit(_ ? pulse.SOURCE : pulse.ADD, t => {
      t = (0,require$vega_util.toNumber)(field(t));
      t != null && (t < min && (min = t), t > max && (max = t));
    });
    Number.isFinite(min) && Number.isFinite(max) || ((extent = (0,require$vega_util.accessorName)(field)) && (extent = ` for field "${extent}"`), pulse.dataflow.warn(`Infinite extent${extent}: [${min}, ${max}]`), min = max = void 0);
    this.value = [min, max];
  }});
  (0,require$vega_util.inherits)(Subflow, require$vega_dataflow.Operator, {connect(target) {
    this.detachSubflow = target.detachSubflow;
    this.targets().add(target);
    return target.source = this;
  }, add(t) {
    this.count += 1;
    this.value.add.push(t);
  }, rem(t) {
    --this.count;
    this.value.rem.push(t);
  }, mod(t) {
    this.value.mod.push(t);
  }, init(pulse) {
    this.value.init(pulse, pulse.NO_SOURCE);
  }, evaluate() {
    return this.value;
  }});
  (0,require$vega_util.inherits)(Facet, require$vega_dataflow.Transform, {activate(flow) {
    this._targets[this._targets.active++] = flow;
  }, subflow(key, flow, pulse, parent) {
    const flows = this.value;
    let sf = (0,require$vega_util.hasOwnProperty)(flows, key) && flows[key], p;
    sf ? sf.value.stamp < pulse.stamp && (sf.init(pulse), this.activate(sf)) : (p = parent || (p = this._group[key]) && p.tuple, parent = pulse.dataflow, sf = new Subflow(pulse.fork(pulse.NO_SOURCE), this), parent.add(sf).connect(flow(parent, key, p)), flows[key] = sf, this.activate(sf));
    return sf;
  }, clean() {
    var flows = this.value;
    let detached = 0;
    for (const key in flows) {
      if (flows[key].count === 0) {
        const detach = flows[key].detachSubflow;
        detach && detach();
        delete flows[key];
        ++detached;
      }
    }
    detached && (flows = this._targets.filter(sf => sf && sf.count > 0), this.initTargets(flows));
  }, initTargets(act) {
    const a = this._targets, n = a.length, m = act ? act.length : 0;
    let i = 0;
    for (; i < m; ++i) {
      a[i] = act[i];
    }
    for (; i < n && a[i] != null; ++i) {
      a[i] = null;
    }
    a.active = m;
  }, transform(_, pulse) {
    const df = pulse.dataflow, key = _.key, flow = _.subflow, cache = this._keys, rekey = _.modified("key"), subflow = key => this.subflow(key, flow, pulse);
    this._group = _.group || {};
    this.initTargets();
    pulse.visit(pulse.REM, t => {
      const id = (0,require$vega_dataflow.tupleid)(t), k = cache.get(id);
      k !== void 0 && (cache.delete(id), subflow(k).rem(t));
    });
    pulse.visit(pulse.ADD, t => {
      const k = key(t);
      cache.set((0,require$vega_dataflow.tupleid)(t), k);
      subflow(k).add(t);
    });
    rekey || pulse.modified(key.fields) ? pulse.visit(pulse.MOD, t => {
      const id = (0,require$vega_dataflow.tupleid)(t), k0 = cache.get(id), k1 = key(t);
      k0 === k1 ? subflow(k1).mod(t) : (cache.set(id, k1), subflow(k0).rem(t), subflow(k1).add(t));
    }) : pulse.changed(pulse.MOD) && pulse.visit(pulse.MOD, t => {
      subflow(cache.get((0,require$vega_dataflow.tupleid)(t))).mod(t);
    });
    rekey && pulse.visit(pulse.REFLOW, t => {
      const id = (0,require$vega_dataflow.tupleid)(t), k0 = cache.get(id), k1 = key(t);
      k0 !== k1 && (cache.set(id, k1), subflow(k0).rem(t), subflow(k1).add(t));
    });
    pulse.clean() ? df.runAfter(() => {
      this.clean();
      cache.clean();
    }) : cache.empty > df.cleanThreshold && df.runAfter(cache.clean);
    return pulse;
  }});
  (0,require$vega_util.inherits)(Field, require$vega_dataflow.Operator);
  Filter.Definition = {type:"Filter", metadata:{changes:!0}, params:[{name:"expr", type:"expr", required:!0}]};
  (0,require$vega_util.inherits)(Filter, require$vega_dataflow.Transform, {transform(_, pulse) {
    function revisit(t) {
      const id = (0,require$vega_dataflow.tupleid)(t), b = test(t, _), s = cache.get(id);
      b && s ? (cache.delete(id), add.push(t)) : b || s ? isMod && b && !s && mod.push(t) : (cache.set(id, 1), rem.push(t));
    }
    const df = pulse.dataflow, cache = this.value, output = pulse.fork(), add = output.add, rem = output.rem, mod = output.mod, test = _.expr;
    let isMod = !0;
    pulse.visit(pulse.REM, t => {
      const id = (0,require$vega_dataflow.tupleid)(t);
      cache.has(id) ? cache.delete(id) : rem.push(t);
    });
    pulse.visit(pulse.ADD, t => {
      test(t, _) ? add.push(t) : cache.set((0,require$vega_dataflow.tupleid)(t), 1);
    });
    pulse.visit(pulse.MOD, revisit);
    _.modified() && (isMod = !1, pulse.visit(pulse.REFLOW, revisit));
    cache.empty > df.cleanThreshold && df.runAfter(cache.clean);
    return output;
  }});
  Flatten.Definition = {type:"Flatten", metadata:{generates:!0}, params:[{name:"fields", type:"field", array:!0, required:!0}, {name:"index", type:"string"}, {name:"as", type:"string", array:!0}]};
  (0,require$vega_util.inherits)(Flatten, require$vega_dataflow.Transform, {transform(_, pulse) {
    const out = pulse.fork(pulse.NO_SOURCE), fields = _.fields, as = fieldNames(fields, _.as || []), index = _.index || null, m = as.length;
    out.rem = this.value;
    pulse.visit(pulse.SOURCE, t => {
      const arrays = fields.map(f => f(t)), maxlen = arrays.reduce((l, a) => Math.max(l, a.length), 0);
      let i = 0, j, d, v;
      for (; i < maxlen; ++i) {
        d = (0,require$vega_dataflow.derive)(t);
        for (j = 0; j < m; ++j) {
          d[as[j]] = (v = arrays[j][i]) == null ? null : v;
        }
        index && (d[index] = i);
        out.add.push(d);
      }
    });
    this.value = out.source = out.add;
    index && out.modifies(index);
    return out.modifies(as);
  }});
  Fold.Definition = {type:"Fold", metadata:{generates:!0}, params:[{name:"fields", type:"field", array:!0, required:!0}, {name:"as", type:"string", array:!0, length:2, "default":["key", "value"]}]};
  (0,require$vega_util.inherits)(Fold, require$vega_dataflow.Transform, {transform(_, pulse) {
    const out = pulse.fork(pulse.NO_SOURCE), fields = _.fields, fnames = fields.map(require$vega_util.accessorName);
    _ = _.as || ["key", "value"];
    const k = _[0], v = _[1], n = fields.length;
    out.rem = this.value;
    pulse.visit(pulse.SOURCE, t => {
      for (let i = 0, d; i < n; ++i) {
        d = (0,require$vega_dataflow.derive)(t), d[k] = fnames[i], d[v] = fields[i](t), out.add.push(d);
      }
    });
    this.value = out.source = out.add;
    return out.modifies(_);
  }});
  Formula.Definition = {type:"Formula", metadata:{modifies:!0}, params:[{name:"expr", type:"expr", required:!0}, {name:"as", type:"string", required:!0}, {name:"initonly", type:"boolean"}]};
  (0,require$vega_util.inherits)(Formula, require$vega_dataflow.Transform, {transform(_, pulse) {
    const func = _.expr, as = _.as, mod = _.modified(), flag = _.initonly ? pulse.ADD : mod ? pulse.SOURCE : pulse.modified(func.fields) || pulse.modified(as) ? pulse.ADD_MOD : pulse.ADD;
    mod && (pulse = pulse.materialize().reflow(!0));
    _.initonly || pulse.modifies(as);
    return pulse.visit(flag, t => t[as] = func(t, _));
  }});
  (0,require$vega_util.inherits)(Generate, require$vega_dataflow.Transform, {transform(_, pulse) {
    pulse = pulse.fork(pulse.ALL);
    const gen = _.generator;
    let data = this.value, num = _.size - data.length, add, t;
    if (num > 0) {
      for (add = []; --num >= 0;) {
        add.push(t = (0,require$vega_dataflow.ingest)(gen(_))), data.push(t);
      }
      pulse.add = pulse.add.length ? pulse.materialize(pulse.ADD).add.concat(add) : add;
    } else {
      _ = data.slice(0, -num), pulse.rem = pulse.rem.length ? pulse.materialize(pulse.REM).rem.concat(_) : _, data = data.slice(-num);
    }
    pulse.source = this.value = data;
    return pulse;
  }});
  const Methods = {value:"value", median:require$d3_array.median, mean:require$d3_array.mean, min:require$d3_array.min, max:require$d3_array.max}, Empty = [];
  Impute.Definition = {type:"Impute", metadata:{changes:!0}, params:[{name:"field", type:"field", required:!0}, {name:"key", type:"field", required:!0}, {name:"keyvals", array:!0}, {name:"groupby", type:"field", array:!0}, {name:"method", type:"enum", "default":"value", values:["value", "mean", "median", "max", "min"]}, {name:"value", "default":0}]};
  (0,require$vega_util.inherits)(Impute, require$vega_dataflow.Transform, {transform(_, pulse) {
    var out = pulse.fork(pulse.ALL), impute = getValue(_), field = getField(_), fName = (0,require$vega_util.accessorName)(_.field), kName = (0,require$vega_util.accessorName)(_.key), gNames = (_.groupby || []).map(require$vega_util.accessorName);
    _ = partition(pulse.source, _.groupby, _.key, _.keyvals);
    pulse = [];
    var prev = this.value, m = _.domain.length, j, l, n;
    var g = 0;
    for (l = _.length; g < l; ++g) {
      var group = _[g];
      var gVals = group.values;
      var value = NaN;
      for (j = 0; j < m; ++j) {
        if (group[j] == null) {
          var kVal = _.domain[j];
          var t = {_impute:!0};
          var i = 0;
          for (n = gVals.length; i < n; ++i) {
            t[gNames[i]] = gVals[i];
          }
          t[kName] = kVal;
          t[fName] = Number.isNaN(value) ? value = impute(group, field) : value;
          pulse.push((0,require$vega_dataflow.ingest)(t));
        }
      }
    }
    pulse.length && (out.add = out.materialize(out.ADD).add.concat(pulse));
    prev.length && (out.rem = out.materialize(out.REM).rem.concat(prev));
    this.value = pulse;
    return out;
  }});
  JoinAggregate.Definition = {type:"JoinAggregate", metadata:{modifies:!0}, params:[{name:"groupby", type:"field", array:!0}, {name:"fields", type:"field", "null":!0, array:!0}, {name:"ops", type:"enum", array:!0, values:require}, {name:"as", type:"string", "null":!0, array:!0}, {name:"key", type:"field"}]};
  (0,require$vega_util.inherits)(JoinAggregate, Aggregate, {transform(_, pulse) {
    const aggr = this, mod = _.modified();
    let cells;
    aggr.value && (mod || pulse.modified(aggr._inputs, !0)) ? (cells = aggr.value = mod ? aggr.init(_) : {}, pulse.visit(pulse.SOURCE, t => aggr.add(t))) : (cells = aggr.value = aggr.value || this.init(_), pulse.visit(pulse.REM, t => aggr.rem(t)), pulse.visit(pulse.ADD, t => aggr.add(t)));
    aggr.changes();
    pulse.visit(pulse.SOURCE, t => {
      (0,require$vega_util.extend)(t, cells[aggr.cellkey(t)].tuple);
    });
    return pulse.reflow(mod).modifies(this._outputs);
  }, changes() {
    const adds = this._adds, mods = this._mods;
    let i, n;
    i = 0;
    for (n = this._alen; i < n; ++i) {
      this.celltuple(adds[i]), adds[i] = null;
    }
    i = 0;
    for (n = this._mlen; i < n; ++i) {
      this.celltuple(mods[i]), mods[i] = null;
    }
    this._alen = this._mlen = 0;
  }});
  KDE.Definition = {type:"KDE", metadata:{generates:!0}, params:[{name:"groupby", type:"field", array:!0}, {name:"field", type:"field", required:!0}, {name:"cumulative", type:"boolean", "default":!1}, {name:"counts", type:"boolean", "default":!1}, {name:"bandwidth", type:"number", "default":0}, {name:"extent", type:"number", array:!0, length:2}, {name:"resolve", type:"enum", values:["shared", "independent"], "default":"independent"}, {name:"steps", type:"number"}, {name:"minsteps", type:"number", 
  "default":25}, {name:"maxsteps", type:"number", "default":200}, {name:"as", type:"string", array:!0, "default":["value", "density"]}]};
  (0,require$vega_util.inherits)(KDE, require$vega_dataflow.Transform, {transform(_, pulse) {
    const out = pulse.fork(pulse.NO_SOURCE | pulse.NO_FIELDS);
    if (!this.value || pulse.changed() || _.modified()) {
      pulse = pulse.materialize(pulse.SOURCE).source;
      const groups = partition$1(pulse, _.groupby, _.field), names = (_.groupby || []).map(require$vega_util.accessorName), bandwidth = _.bandwidth, method = _.cumulative ? "cdf" : "pdf", as = _.as || ["value", "density"], values = [];
      let domain = _.extent, minsteps = _.steps || _.minsteps || 25, maxsteps = _.steps || _.maxsteps || 200;
      method !== "pdf" && method !== "cdf" && (0,require$vega_util.error)("Invalid density method: " + method);
      _.resolve === "shared" && (domain ||= (0,require$vega_util.extent)(pulse, _.field), minsteps = maxsteps = _.steps || maxsteps);
      groups.forEach(g => {
        const density = (0,require$vega_statistics.randomKDE)(g, bandwidth)[method], scale = _.counts ? g.length : 1, local = domain || (0,require$vega_util.extent)(g);
        (0,require$vega_statistics.sampleCurve)(density, local, minsteps, maxsteps).forEach(v => {
          const t = {};
          for (let i = 0; i < names.length; ++i) {
            t[names[i]] = g.dims[i];
          }
          t[as[0]] = v[0];
          t[as[1]] = v[1] * scale;
          values.push((0,require$vega_dataflow.ingest)(t));
        });
      });
      this.value && (out.rem = this.value);
      this.value = out.add = out.source = values;
    }
    return out;
  }});
  (0,require$vega_util.inherits)(Key, require$vega_dataflow.Operator);
  (0,require$vega_util.inherits)(Load, require$vega_dataflow.Transform, {transform(_, pulse) {
    const df = pulse.dataflow;
    return this._pending ? output(this, pulse, this._pending) : stop(_) ? pulse.StopPropagation : _.values ? output(this, pulse, df.parse(_.values, _.format)) : _.async ? {async:df.request(_.url, _.format).then(res => {
      this._pending = (0,require$vega_util.array)(res.data);
      return df => df.touch(this);
    })} : df.request(_.url, _.format).then(res => output(this, pulse, (0,require$vega_util.array)(res.data)));
  }});
  Lookup.Definition = {type:"Lookup", metadata:{modifies:!0}, params:[{name:"index", type:"index", params:[{name:"from", type:"data", required:!0}, {name:"key", type:"field", required:!0}]}, {name:"values", type:"field", array:!0}, {name:"fields", type:"field", array:!0, required:!0}, {name:"as", type:"string", array:!0}, {name:"default", "default":null}]};
  (0,require$vega_util.inherits)(Lookup, require$vega_dataflow.Transform, {transform(_, pulse) {
    const keys = _.fields, index = _.index, values = _.values, defaultValue = _.default == null ? null : _.default;
    var reset = _.modified();
    const n = keys.length;
    let flag = reset ? pulse.SOURCE : pulse.ADD, out = pulse, as = _.as, m;
    values ? (m = values.length, n > 1 && !as && (0,require$vega_util.error)('Multi-field lookup requires explicit "as" parameter.'), as && as.length !== n * m && (0,require$vega_util.error)('The "as" parameter has too few output field names.'), as = as || values.map(require$vega_util.accessorName), _ = function(t) {
      for (var i = 0, k = 0, j, v; i < n; ++i) {
        if (v = index.get(keys[i](t)), v == null) {
          for (j = 0; j < m; ++j, ++k) {
            t[as[k]] = defaultValue;
          }
        } else {
          for (j = 0; j < m; ++j, ++k) {
            t[as[k]] = values[j](v);
          }
        }
      }
    }) : (as || (0,require$vega_util.error)("Missing output field names."), _ = function(t) {
      for (var i = 0, v; i < n; ++i) {
        v = index.get(keys[i](t)), t[as[i]] = v == null ? defaultValue : v;
      }
    });
    reset ? out = pulse.reflow(!0) : (reset = keys.some(k => pulse.modified(k.fields)), flag |= reset ? pulse.MOD : 0);
    pulse.visit(flag, _);
    return out.modifies(as);
  }});
  (0,require$vega_util.inherits)(MultiExtent, require$vega_dataflow.Operator);
  (0,require$vega_util.inherits)(MultiValues, require$vega_dataflow.Operator);
  (0,require$vega_util.inherits)(Params, require$vega_dataflow.Transform, {transform(_, pulse) {
    this.modified(_.modified());
    this.value = _;
    return pulse.fork(pulse.NO_SOURCE | pulse.NO_FIELDS);
  }});
  Pivot.Definition = {type:"Pivot", metadata:{generates:!0, changes:!0}, params:[{name:"groupby", type:"field", array:!0}, {name:"field", type:"field", required:!0}, {name:"value", type:"field", required:!0}, {name:"op", type:"enum", values:require, "default":"sum"}, {name:"limit", type:"number", "default":0}, {name:"key", type:"field"}]};
  (0,require$vega_util.inherits)(Pivot, Aggregate, {_transform:Aggregate.prototype.transform, transform(_, pulse) {
    return this._transform(aggregateParams(_, pulse), pulse);
  }});
  (0,require$vega_util.inherits)(PreFacet, Facet, {transform(_, pulse) {
    const flow = _.subflow, field = _.field, subflow = t => this.subflow((0,require$vega_dataflow.tupleid)(t), flow, pulse, t);
    (_.modified("field") || field && pulse.modified((0,require$vega_util.accessorFields)(field))) && (0,require$vega_util.error)("PreFacet does not support field modification.");
    this.initTargets();
    field ? (pulse.visit(pulse.MOD, t => {
      const sf = subflow(t);
      field(t).forEach(_ => sf.mod(_));
    }), pulse.visit(pulse.ADD, t => {
      const sf = subflow(t);
      field(t).forEach(_ => sf.add((0,require$vega_dataflow.ingest)(_)));
    }), pulse.visit(pulse.REM, t => {
      const sf = subflow(t);
      field(t).forEach(_ => sf.rem(_));
    })) : (pulse.visit(pulse.MOD, t => subflow(t).mod(t)), pulse.visit(pulse.ADD, t => subflow(t).add(t)), pulse.visit(pulse.REM, t => subflow(t).rem(t)));
    pulse.clean() && pulse.runAfter(() => this.clean());
    return pulse;
  }});
  Project.Definition = {type:"Project", metadata:{generates:!0, changes:!0}, params:[{name:"fields", type:"field", array:!0}, {name:"as", type:"string", "null":!0, array:!0}]};
  (0,require$vega_util.inherits)(Project, require$vega_dataflow.Transform, {transform(_, pulse) {
    const out = pulse.fork(pulse.NO_SOURCE), fields = _.fields, as = fieldNames(_.fields, _.as || []), derive = fields ? (s, t) => {
      for (let i = 0, n = fields.length; i < n; ++i) {
        t[as[i]] = fields[i](s);
      }
      return t;
    } : require$vega_dataflow.rederive;
    let lut;
    this.value ? lut = this.value : (pulse = pulse.addAll(), lut = this.value = {});
    pulse.visit(pulse.REM, t => {
      t = (0,require$vega_dataflow.tupleid)(t);
      out.rem.push(lut[t]);
      lut[t] = null;
    });
    pulse.visit(pulse.ADD, t => {
      const dt = derive(t, (0,require$vega_dataflow.ingest)({}));
      lut[(0,require$vega_dataflow.tupleid)(t)] = dt;
      out.add.push(dt);
    });
    pulse.visit(pulse.MOD, t => {
      out.mod.push(derive(t, lut[(0,require$vega_dataflow.tupleid)(t)]));
    });
    return out;
  }});
  (0,require$vega_util.inherits)(Proxy, require$vega_dataflow.Transform, {transform(_, pulse) {
    this.value = _.value;
    return _.modified("value") ? pulse.fork(pulse.NO_SOURCE | pulse.NO_FIELDS) : pulse.StopPropagation;
  }});
  Quantile.Definition = {type:"Quantile", metadata:{generates:!0, changes:!0}, params:[{name:"groupby", type:"field", array:!0}, {name:"field", type:"field", required:!0}, {name:"probs", type:"number", array:!0}, {name:"step", type:"number", "default":0.01}, {name:"as", type:"string", array:!0, "default":["prob", "value"]}]};
  (0,require$vega_util.inherits)(Quantile, require$vega_dataflow.Transform, {transform(_, pulse) {
    const out = pulse.fork(pulse.NO_SOURCE | pulse.NO_FIELDS), as = _.as || ["prob", "value"];
    if (this.value && !_.modified() && !pulse.changed()) {
      return out.source = this.value, out;
    }
    pulse = pulse.materialize(pulse.SOURCE).source;
    pulse = partition$1(pulse, _.groupby, _.field);
    const names = (_.groupby || []).map(require$vega_util.accessorName), values = [], step = _.step || 0.01, p = _.probs || (0,require$d3_array.range)(step / 2, 1 - 1e-14, step), n = p.length;
    pulse.forEach(g => {
      const q = (0,require$vega_statistics.quantiles)(g, p);
      for (let i = 0; i < n; ++i) {
        const t = {};
        for (let i = 0; i < names.length; ++i) {
          t[names[i]] = g.dims[i];
        }
        t[as[0]] = p[i];
        t[as[1]] = q[i];
        values.push((0,require$vega_dataflow.ingest)(t));
      }
    });
    this.value && (out.rem = this.value);
    this.value = out.add = out.source = values;
    return out;
  }});
  (0,require$vega_util.inherits)(Relay, require$vega_dataflow.Transform, {transform(_, pulse) {
    let out, lut;
    this.value ? lut = this.value : (out = pulse = pulse.addAll(), lut = this.value = {});
    _.derive && (out = pulse.fork(pulse.NO_SOURCE), pulse.visit(pulse.REM, t => {
      t = (0,require$vega_dataflow.tupleid)(t);
      out.rem.push(lut[t]);
      lut[t] = null;
    }), pulse.visit(pulse.ADD, t => {
      const dt = (0,require$vega_dataflow.derive)(t);
      lut[(0,require$vega_dataflow.tupleid)(t)] = dt;
      out.add.push(dt);
    }), pulse.visit(pulse.MOD, t => {
      const dt = lut[(0,require$vega_dataflow.tupleid)(t)];
      for (const k in t) {
        dt[k] = t[k], out.modifies(k);
      }
      out.mod.push(dt);
    }));
    return out;
  }});
  Sample.Definition = {type:"Sample", metadata:{}, params:[{name:"size", type:"number", "default":1000}]};
  (0,require$vega_util.inherits)(Sample, require$vega_dataflow.Transform, {transform(_, pulse) {
    function update(t) {
      let p, idx;
      res.length < num ? res.push(t) : (idx = ~~((cnt + 1) * (0,require$vega_statistics.random)()), idx < res.length && idx >= cap && (p = res[idx], map[(0,require$vega_dataflow.tupleid)(p)] && out.rem.push(p), res[idx] = t));
      ++cnt;
    }
    const out = pulse.fork(pulse.NO_SOURCE);
    var mod = _.modified("size");
    const num = _.size, map = this.value.reduce((m, t) => (m[(0,require$vega_dataflow.tupleid)(t)] = 1, m), {});
    let res = this.value, cnt = this.count, cap = 0;
    pulse.rem.length && (pulse.visit(pulse.REM, t => {
      const id = (0,require$vega_dataflow.tupleid)(t);
      map[id] && (map[id] = -1, out.rem.push(t));
      --cnt;
    }), res = res.filter(t => map[(0,require$vega_dataflow.tupleid)(t)] !== -1));
    (pulse.rem.length || mod) && res.length < num && pulse.source && (cap = cnt = res.length, pulse.visit(pulse.SOURCE, t => {
      map[(0,require$vega_dataflow.tupleid)(t)] || update(t);
    }), cap = -1);
    if (mod && res.length > num) {
      _ = res.length - num;
      for (mod = 0; mod < _; ++mod) {
        map[(0,require$vega_dataflow.tupleid)(res[mod])] = -1, out.rem.push(res[mod]);
      }
      res = res.slice(_);
    }
    pulse.mod.length && pulse.visit(pulse.MOD, t => {
      map[(0,require$vega_dataflow.tupleid)(t)] && out.mod.push(t);
    });
    pulse.add.length && pulse.visit(pulse.ADD, update);
    if (pulse.add.length || cap < 0) {
      out.add = res.filter(t => !map[(0,require$vega_dataflow.tupleid)(t)]);
    }
    this.count = cnt;
    this.value = out.source = res;
    return out;
  }});
  Sequence.Definition = {type:"Sequence", metadata:{generates:!0, changes:!0}, params:[{name:"start", type:"number", required:!0}, {name:"stop", type:"number", required:!0}, {name:"step", type:"number", "default":1}, {name:"as", type:"string", "default":"data"}]};
  (0,require$vega_util.inherits)(Sequence, require$vega_dataflow.Transform, {transform(_, pulse) {
    if (!this.value || _.modified()) {
      var out = pulse.materialize().fork(pulse.MOD), as = _.as || "data";
      out.rem = this.value ? pulse.rem.concat(this.value) : pulse.rem;
      this.value = (0,require$d3_array.range)(_.start, _.stop, _.step || 1).map(v => {
        const t = {};
        t[as] = v;
        return (0,require$vega_dataflow.ingest)(t);
      });
      out.add = pulse.add.concat(this.value);
      return out;
    }
  }});
  (0,require$vega_util.inherits)(Sieve, require$vega_dataflow.Transform, {transform(_, pulse) {
    this.value = pulse.source;
    return pulse.changed() ? pulse.fork(pulse.NO_SOURCE | pulse.NO_FIELDS) : pulse.StopPropagation;
  }});
  const OUTPUT = ["unit0", "unit1"];
  TimeUnit.Definition = {type:"TimeUnit", metadata:{modifies:!0}, params:[{name:"field", type:"field", required:!0}, {name:"interval", type:"boolean", "default":!0}, {name:"units", type:"enum", values:require$vega_time.TIME_UNITS, array:!0}, {name:"step", type:"number", "default":1}, {name:"maxbins", type:"number", "default":40}, {name:"extent", type:"date", array:!0}, {name:"timezone", type:"enum", "default":"local", values:["local", "utc"]}, {name:"as", type:"string", array:!0, length:2, "default":OUTPUT}]};
  (0,require$vega_util.inherits)(TimeUnit, require$vega_dataflow.Transform, {transform(_, pulse) {
    const field = _.field, band = _.interval !== !1;
    var utc = _.timezone === "utc";
    const floor = this._floor(_, pulse), offset = (utc ? require$vega_time.utcInterval : require$vega_time.timeInterval)(floor.unit).offset;
    utc = _.as || OUTPUT;
    const u0 = utc[0], u1 = utc[1], step = floor.step;
    let min = floor.start || Infinity, max = floor.stop || -Infinity, flag = pulse.ADD;
    if (_.modified() || pulse.changed(pulse.REM) || pulse.modified((0,require$vega_util.accessorFields)(field))) {
      pulse = pulse.reflow(!0), flag = pulse.SOURCE, min = Infinity, max = -Infinity;
    }
    pulse.visit(flag, t => {
      var v = field(t);
      let b;
      v == null ? (t[u0] = null, band && (t[u1] = null)) : (t[u0] = v = b = floor(v), band && (t[u1] = b = offset(v, step)), v < min && (min = v), b > max && (max = b));
    });
    floor.start = min;
    floor.stop = max;
    return pulse.modifies(band ? utc : u0);
  }, _floor(_, pulse) {
    var utc = _.timezone === "utc";
    const {units, step} = _.units ? {units:_.units, step:_.step || 1} : (0,require$vega_time.timeBin)({extent:_.extent || (0,require$vega_util.extent)(pulse.materialize(pulse.SOURCE).source, _.field), maxbins:_.maxbins});
    _ = (0,require$vega_time.timeUnits)(units);
    pulse = this.value || {};
    utc = (utc ? require$vega_time.utcFloor : require$vega_time.timeFloor)(_, step);
    utc.unit = (0,require$vega_util.peek)(_);
    utc.units = _;
    utc.step = step;
    utc.start = pulse.start;
    utc.stop = pulse.stop;
    return this.value = utc;
  }});
  (0,require$vega_util.inherits)(TupleIndex, require$vega_dataflow.Transform, {transform(_, pulse) {
    const df = pulse.dataflow, field = _.field, index = this.value, set = t => index.set(field(t), t);
    let mod = !0;
    _.modified("field") || pulse.modified(field.fields) ? (index.clear(), pulse.visit(pulse.SOURCE, set)) : pulse.changed() ? (pulse.visit(pulse.REM, t => index.delete(field(t))), pulse.visit(pulse.ADD, set)) : mod = !1;
    this.modified(mod);
    index.empty > df.cleanThreshold && df.runAfter(index.clean);
    return pulse.fork();
  }});
  (0,require$vega_util.inherits)(Values, require$vega_dataflow.Transform, {transform(_, pulse) {
    if (!this.value || _.modified("field") || _.modified("sort") || pulse.changed() || _.sort && pulse.modified(_.sort.fields)) {
      this.value = (_.sort ? pulse.source.slice().sort((0,require$vega_dataflow.stableCompare)(_.sort)) : pulse.source).map(_.field);
    }
  }});
  const WindowOps = {row_number:function() {
    return {next:w => w.index + 1};
  }, rank:function() {
    let rank;
    return {init:() => rank = 1, next:w => {
      const i = w.index, data = w.data;
      return i && w.compare(data[i - 1], data[i]) ? rank = i + 1 : rank;
    }};
  }, dense_rank:function() {
    let drank;
    return {init:() => drank = 1, next:w => {
      const i = w.index, d = w.data;
      return i && w.compare(d[i - 1], d[i]) ? ++drank : drank;
    }};
  }, percent_rank:function() {
    const rank = WindowOps.rank(), next = rank.next;
    return {init:rank.init, next:w => (next(w) - 1) / (w.data.length - 1)};
  }, cume_dist:function() {
    let cume;
    return {init:() => cume = 0, next:w => {
      const d = w.data, c = w.compare;
      w = w.index;
      if (cume < w) {
        for (; w + 1 < d.length && !c(d[w], d[w + 1]);) {
          ++w;
        }
        cume = w;
      }
      return (1 + cume) / d.length;
    }};
  }, ntile:function(field, num) {
    num = +num;
    num > 0 || (0,require$vega_util.error)("ntile num must be greater than zero.");
    field = WindowOps.cume_dist();
    const next = field.next;
    return {init:field.init, next:w => Math.ceil(num * next(w))};
  }, lag:function(field, offset) {
    offset = +offset || 1;
    return {next:w => {
      const i = w.index - offset;
      return i >= 0 ? field(w.data[i]) : null;
    }};
  }, lead:function(field, offset) {
    offset = +offset || 1;
    return {next:w => {
      const i = w.index + offset;
      w = w.data;
      return i < w.length ? field(w[i]) : null;
    }};
  }, first_value:function(field) {
    return {next:w => field(w.data[w.i0])};
  }, last_value:function(field) {
    return {next:w => field(w.data[w.i1 - 1])};
  }, nth_value:function(field, nth) {
    nth = +nth;
    nth > 0 || (0,require$vega_util.error)("nth_value nth must be greater than zero.");
    return {next:w => {
      const i = w.i0 + (nth - 1);
      return i < w.i1 ? field(w.data[i]) : null;
    }};
  }, prev_value:function(field) {
    let prev;
    return {init:() => prev = null, next:w => {
      w = field(w.data[w.index]);
      return w != null ? prev = w : prev;
    }};
  }, next_value:function(field) {
    let v, i;
    return {init:() => (v = null, i = -1), next:w => {
      var d = w.data;
      if (w.index <= i) {
        d = v;
      } else {
        a: {
          w = w.index;
          for (let n = d.length; w < n; ++w) {
            if (field(d[w]) != null) {
              break a;
            }
          }
          w = -1;
        }
        d = (i = w) < 0 ? (i = d.length, v = null) : v = field(d[i]);
      }
      return d;
    }};
  }};
  module = Object.keys(WindowOps);
  exports = WindowState.prototype;
  exports.init = function() {
    this.windows.forEach(_ => _.init());
    this.cell && this.cell.init();
  };
  exports.update = function(w, t) {
    const cell = this.cell, wind = this.windows, data = w.data, m = wind && wind.length;
    let j;
    if (cell) {
      for (j = w.p0; j < w.i0; ++j) {
        cell.rem(data[j]);
      }
      for (j = w.p1; j < w.i1; ++j) {
        cell.add(data[j]);
      }
      cell.set(t);
    }
    for (j = 0; j < m; ++j) {
      wind[j].update(w, t);
    }
  };
  Window.Definition = {type:"Window", metadata:{modifies:!0}, params:[{name:"sort", type:"compare"}, {name:"groupby", type:"field", array:!0}, {name:"ops", type:"enum", array:!0, values:module.concat(require)}, {name:"params", type:"number", "null":!0, array:!0}, {name:"aggregate_params", type:"number", "null":!0, array:!0}, {name:"fields", type:"field", "null":!0, array:!0}, {name:"as", type:"string", "null":!0, array:!0}, {name:"frame", type:"number", "null":!0, array:!0, length:2, "default":[null, 
  0]}, {name:"ignorePeers", type:"boolean", "default":!1}]};
  (0,require$vega_util.inherits)(Window, require$vega_dataflow.Transform, {transform(_, pulse) {
    this.stamp = pulse.stamp;
    const mod = _.modified(), cmp = (0,require$vega_dataflow.stableCompare)(_.sort), key = groupkey(_.groupby), group = t => this.group(key(t));
    let state = this.state;
    if (!state || mod) {
      state = this.state = new WindowState(_);
    }
    mod || pulse.modified(state.inputs) ? (this.value = {}, pulse.visit(pulse.SOURCE, t => group(t).add(t))) : (pulse.visit(pulse.REM, t => group(t).remove(t)), pulse.visit(pulse.ADD, t => group(t).add(t)));
    for (let i = 0, n = this._mlen; i < n; ++i) {
      processPartition(this._mods[i], state, cmp, _);
    }
    this._mlen = 0;
    this._mods = [];
    return pulse.reflow(mod).modifies(state.outputs);
  }, group(key) {
    let group = this.value[key];
    group || (group = this.value[key] = SortedList(require$vega_dataflow.tupleid), group.stamp = -1);
    group.stamp < this.stamp && (group.stamp = this.stamp, this._mods[this._mlen++] = group);
    return group;
  }});
};

//# sourceMappingURL=module$node_modules$vega_transforms$build$vega_transforms.js.map
