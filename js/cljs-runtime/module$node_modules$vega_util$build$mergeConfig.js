shadow$provide.module$node_modules$vega_util$build$mergeConfig = function(require, module, exports) {
  function mergeConfig(...configs) {
    return configs.reduce((out, source) => {
      for (const key in source) {
        key === "signals" ? out.signals = mergeNamed(out.signals, source.signals) : writeConfig(out, key, source[key], key === "legend" ? {layout:1} : key === "style" ? !0 : null);
      }
      return out;
    }, {});
  }
  function writeConfig(output, key, value, recurse) {
    if (isLegalKey(key)) {
      var k;
      if ((0,default$$require$_DOT__SLASH_isObject_DOT_js.default)(value) && !(0,require$_DOT__SLASH_isArray_DOT_js.isArray)(value)) {
        for (k in output = (0,default$$require$_DOT__SLASH_isObject_DOT_js.default)(output[key]) ? output[key] : output[key] = {}, value) {
          recurse && (recurse === !0 || recurse[k]) ? writeConfig(output, k, value[k]) : isLegalKey(k) && (output[k] = value[k]);
        }
      } else {
        output[key] = value;
      }
    }
  }
  function mergeNamed(a, b) {
    function add(item) {
      map[item.name] || (map[item.name] = 1, out.push(item));
    }
    if (a == null) {
      return b;
    }
    if (b == null) {
      return a;
    }
    const map = {}, out = [];
    b.forEach(add);
    a.forEach(add);
    return out;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, mergeConfig:{enumerable:!0, get:function() {
    return mergeConfig;
  }}, writeConfig:{enumerable:!0, get:function() {
    return writeConfig;
  }}});
  var require$_DOT__SLASH_isArray_DOT_js = require("module$node_modules$vega_util$build$isArray");
  module = require("module$node_modules$vega_util$build$isObject");
  var default$$require$_DOT__SLASH_isObject_DOT_js = require.esmDefault(module);
  const isLegalKey = key => key !== "__proto__" && key !== "constructor" && key !== "prototype";
};

//# sourceMappingURL=module$node_modules$vega_util$build$mergeConfig.js.map
