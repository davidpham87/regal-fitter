shadow$provide.module$node_modules$d3_array$src$sort = function(require, module, exports) {
  function sort(values, ...F) {
    if (typeof values[Symbol.iterator] !== "function") {
      throw new TypeError("values is not iterable");
    }
    values = Array.from(values);
    let [f] = F;
    if (f && f.length !== 2 || F.length > 1) {
      const index = Uint32Array.from(values, (d, i) => i);
      F.length > 1 ? (F = F.map(f => values.map(f)), index.sort((i, j) => {
        for (const f of F) {
          const c = ascendingDefined(f[i], f[j]);
          if (c) {
            return c;
          }
        }
      })) : (f = values.map(f), index.sort((i, j) => ascendingDefined(f[i], f[j])));
      return (0,default$$require$_DOT__SLASH_permute_DOT_js.default)(values, index);
    }
    return values.sort(compareDefined(f));
  }
  function compareDefined(compare = default$$require$_DOT__SLASH_ascending_DOT_js.default) {
    if (compare === default$$require$_DOT__SLASH_ascending_DOT_js.default) {
      return ascendingDefined;
    }
    if (typeof compare !== "function") {
      throw new TypeError("compare is not a function");
    }
    return (a, b) => {
      const x = compare(a, b);
      return x || x === 0 ? x : (compare(b, b) === 0) - (compare(a, a) === 0);
    };
  }
  function ascendingDefined(a, b) {
    return (a == null || !(a >= a)) - (b == null || !(b >= b)) || (a < b ? -1 : a > b ? 1 : 0);
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, ascendingDefined:{enumerable:!0, get:function() {
    return ascendingDefined;
  }}, compareDefined:{enumerable:!0, get:function() {
    return compareDefined;
  }}, default:{enumerable:!0, get:function() {
    return sort;
  }}});
  module = require("module$node_modules$d3_array$src$ascending");
  exports = require("module$node_modules$d3_array$src$permute");
  var default$$require$_DOT__SLASH_ascending_DOT_js = require.esmDefault(module), default$$require$_DOT__SLASH_permute_DOT_js = require.esmDefault(exports);
};

//# sourceMappingURL=module$node_modules$d3_array$src$sort.js.map
