shadow$provide.module$node_modules$vega_util$build$extent = function(require, module, exports) {
  function extent(array, f) {
    let i = 0, n, v, min, max;
    if (array && (n = array.length)) {
      if (f == null) {
        for (v = array[i]; i < n && (v == null || v !== v); v = array[++i]) {
        }
        for (min = max = v; i < n; ++i) {
          v = array[i], v != null && (v < min && (min = v), v > max && (max = v));
        }
      } else {
        for (v = f(array[i]); i < n && (v == null || v !== v); v = f(array[++i])) {
        }
        for (min = max = v; i < n; ++i) {
          v = f(array[i]), v != null && (v < min && (min = v), v > max && (max = v));
        }
      }
    }
    return [min, max];
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, default:{enumerable:!0, get:function() {
    return extent;
  }}});
};

//# sourceMappingURL=module$node_modules$vega_util$build$extent.js.map
