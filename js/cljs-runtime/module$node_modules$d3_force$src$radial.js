shadow$provide.module$node_modules$d3_force$src$radial = function(require, module, exports) {
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, default:{enumerable:!0, get:function() {
    return $$default;
  }}});
  module = require("module$node_modules$d3_force$src$constant");
  var default$$require$_DOT__SLASH_constant_DOT_js = require.esmDefault(module);
  const $$default = function(radius, x, y) {
    function force(alpha) {
      for (var i = 0, n = nodes.length; i < n; ++i) {
        var node = nodes[i], dx = node.x - x || 1e-6, dy = node.y - y || 1e-6, r = Math.sqrt(dx * dx + dy * dy);
        r = (radiuses[i] - r) * strengths[i] * alpha / r;
        node.vx += dx * r;
        node.vy += dy * r;
      }
    }
    function initialize() {
      if (nodes) {
        var i, n = nodes.length;
        strengths = Array(n);
        radiuses = Array(n);
        for (i = 0; i < n; ++i) {
          radiuses[i] = +radius(nodes[i], i, nodes), strengths[i] = isNaN(radiuses[i]) ? 0 : +strength(nodes[i], i, nodes);
        }
      }
    }
    var nodes, strength = (0,default$$require$_DOT__SLASH_constant_DOT_js.default)(0.1), strengths, radiuses;
    typeof radius !== "function" && (radius = (0,default$$require$_DOT__SLASH_constant_DOT_js.default)(+radius));
    x == null && (x = 0);
    y == null && (y = 0);
    force.initialize = function(_) {
      nodes = _;
      initialize();
    };
    force.strength = function(_) {
      return arguments.length ? (strength = typeof _ === "function" ? _ : (0,default$$require$_DOT__SLASH_constant_DOT_js.default)(+_), initialize(), force) : strength;
    };
    force.radius = function(_) {
      return arguments.length ? (radius = typeof _ === "function" ? _ : (0,default$$require$_DOT__SLASH_constant_DOT_js.default)(+_), initialize(), force) : radius;
    };
    force.x = function(_) {
      return arguments.length ? (x = +_, force) : x;
    };
    force.y = function(_) {
      return arguments.length ? (y = +_, force) : y;
    };
    return force;
  };
};

//# sourceMappingURL=module$node_modules$d3_force$src$radial.js.map
