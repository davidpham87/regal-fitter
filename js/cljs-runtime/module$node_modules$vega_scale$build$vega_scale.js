shadow$provide.module$node_modules$vega_scale$build$vega_scale = function(require, module, exports) {
  function bandSpace(count, paddingInner, paddingOuter) {
    paddingInner = count - paddingInner + paddingOuter * 2;
    return count ? paddingInner > 0 ? paddingInner : 1 : 0;
  }
  function invertRange(scale) {
    return function(_) {
      let lo = _[0];
      _ = _[1];
      let t;
      _ < lo && (t = lo, lo = _, _ = t);
      return [scale.invert(lo), scale.invert(_)];
    };
  }
  function invertRangeExtent(scale) {
    return function(_) {
      const range = scale.range();
      let lo = _[0];
      _ = _[1];
      let min = -1, max;
      let n;
      if (_ < lo) {
        var t = lo;
        lo = _;
        _ = t;
      }
      t = 0;
      for (n = range.length; t < n; ++t) {
        range[t] >= lo && range[t] <= _ && (min < 0 && (min = t), max = t);
      }
      if (!(min < 0)) {
        return lo = scale.invertExtent(range[min]), _ = scale.invertExtent(range[max]), [lo[0] === void 0 ? lo[1] : lo[0], _[1] === void 0 ? _[0] : _[1]];
      }
    };
  }
  function band() {
    function rescale() {
      var n = domain().length;
      const reverse = range$1[1] < range$1[0], stop = range$1[1 - reverse], space = bandSpace(n, paddingInner, paddingOuter);
      let start = range$1[reverse - 0];
      step = (stop - start) / (space || 1);
      round && (step = Math.floor(step));
      start += (stop - start - step * (n - paddingInner)) * align;
      bandwidth = step * (1 - paddingInner);
      round && (start = Math.round(start), bandwidth = Math.round(bandwidth));
      n = (0,require$d3_array.range)(n).map(i => start + step * i);
      return ordinalRange(reverse ? n.reverse() : n);
    }
    const scale = (0,require$d3_scale.scaleOrdinal)().unknown(void 0), domain = scale.domain, ordinalRange = scale.range;
    let range$1 = [0, 1], step, bandwidth, round = !1, paddingInner = 0, paddingOuter = 0, align = 0.5;
    delete scale.unknown;
    scale.domain = function(_) {
      return arguments.length ? (domain(_), rescale()) : domain();
    };
    scale.range = function(_) {
      return arguments.length ? (range$1 = [+_[0], +_[1]], rescale()) : range$1.slice();
    };
    scale.rangeRound = function(_) {
      range$1 = [+_[0], +_[1]];
      round = !0;
      return rescale();
    };
    scale.bandwidth = function() {
      return bandwidth;
    };
    scale.step = function() {
      return step;
    };
    scale.round = function(_) {
      return arguments.length ? (round = !!_, rescale()) : round;
    };
    scale.padding = function(_) {
      return arguments.length ? (paddingInner = paddingOuter = Math.max(0, Math.min(1, _)), rescale()) : paddingInner;
    };
    scale.paddingInner = function(_) {
      return arguments.length ? (paddingInner = Math.max(0, Math.min(1, _)), rescale()) : paddingInner;
    };
    scale.paddingOuter = function(_) {
      return arguments.length ? (paddingOuter = Math.max(0, Math.min(1, _)), rescale()) : paddingOuter;
    };
    scale.align = function(_) {
      return arguments.length ? (align = Math.max(0, Math.min(1, _)), rescale()) : align;
    };
    scale.invertRange = function(_) {
      if (_[0] != null && _[1] != null) {
        var reverse = range$1[1] < range$1[0], values = reverse ? ordinalRange().reverse() : ordinalRange(), n = values.length - 1, lo = +_[0], hi = +_[1];
        if (lo === lo && hi === hi) {
          if (hi < lo) {
            var t = lo;
            lo = hi;
            hi = t;
          }
          if (!(hi < values[0] || lo > range$1[1 - reverse])) {
            return _ = Math.max(0, (0,require$d3_array.bisectRight)(values, lo) - 1), hi = lo === hi ? _ : (0,require$d3_array.bisectRight)(values, hi) - 1, lo - values[_] > bandwidth + 1e-10 && ++_, reverse && (t = _, _ = n - hi, hi = n - t), _ > hi ? void 0 : domain().slice(_, hi + 1);
          }
        }
      }
    };
    scale.invert = function(_) {
      return (_ = scale.invertRange([_, _])) ? _[0] : _;
    };
    scale.copy = function() {
      return band().domain(domain()).range(range$1).round(round).paddingInner(paddingInner).paddingOuter(paddingOuter).align(align);
    };
    return rescale();
  }
  function pointish(scale) {
    const copy = scale.copy;
    scale.padding = scale.paddingOuter;
    delete scale.paddingInner;
    scale.copy = function() {
      return pointish(copy());
    };
    return scale;
  }
  function scaleBinOrdinal() {
    function scale(x) {
      return x == null || x !== x ? void 0 : range[((0,require$d3_array.bisect)(domain, x) - 1) % range.length];
    }
    let domain = [], range = [];
    scale.domain = function(_) {
      return arguments.length ? (domain = map.call(_, require$vega_util.toNumber), scale) : domain.slice();
    };
    scale.range = function(_) {
      return arguments.length ? (range = slice.call(_), scale) : range.slice();
    };
    scale.tickFormat = function(count, specifier) {
      return (0,require$d3_scale.tickFormat)(domain[0], (0,require$vega_util.peek)(domain), count == null ? 10 : count, specifier);
    };
    scale.copy = function() {
      return scaleBinOrdinal().domain(scale.domain()).range(scale.range());
    };
    return scale;
  }
  function registerScale(scale) {
    scale[VEGA_SCALE] = !0;
    return scale;
  }
  function isRegisteredScale(scale) {
    return scale && scale[VEGA_SCALE] === !0;
  }
  function create(type, constructor, metadata) {
    const ctr = function() {
      const s = constructor();
      s.invertRange || (s.invertRange = s.invert ? invertRange(s) : s.invertExtent ? invertRangeExtent(s) : void 0);
      s.type = type;
      return registerScale(s);
    };
    ctr.metadata = (0,require$vega_util.toSet)((0,require$vega_util.array)(metadata));
    return ctr;
  }
  function scale(type, scale, metadata) {
    return arguments.length > 1 ? (scales.set(type, create(type, scale, metadata)), this) : isValidScaleType(type) ? scales.get(type) : void 0;
  }
  function isValidScaleType(type) {
    return scales.has(type);
  }
  function hasType(key, type) {
    return (key = scales.get(key)) && key.metadata[type];
  }
  function isContinuous(key) {
    return hasType(key, "continuous");
  }
  function isDiscrete(key) {
    return hasType(key, "discrete");
  }
  function isDiscretizing(key) {
    return hasType(key, "discretizing");
  }
  function isLogarithmic(key) {
    return hasType(key, "log");
  }
  function isTemporal(key) {
    return hasType(key, "temporal");
  }
  function isInterpolating(key) {
    return hasType(key, "interpolating");
  }
  function isQuantile(key) {
    return hasType(key, "quantile");
  }
  function interpolateRange(interpolator, range) {
    const start = range[0], span = (0,require$vega_util.peek)(range) - start;
    return function(i) {
      return interpolator(start + i * span);
    };
  }
  function interpolateColors(colors, type, gamma) {
    return $$1.piecewise(interpolate(type || "rgb", gamma), colors);
  }
  function quantizeInterpolator(interpolator, count) {
    const samples = Array(count), n = count + 1;
    for (let i = 0; i < count;) {
      samples[i] = interpolator(++i / n);
    }
    return samples;
  }
  function scaleCopy(scale) {
    const t = scale.type;
    scale = scale.copy();
    scale.type = t;
    return scale;
  }
  function scaleFraction(scale$1, min, max) {
    var delta = max - min;
    let t, s;
    return delta && Number.isFinite(delta) ? (delta = (t = scale$1.type).indexOf("-"), t = delta < 0 ? t : t.slice(delta + 1), s = scale(t)().domain([min, max]).range([0, 1]), scaleProps.forEach(m => scale$1[m] ? s[m](scale$1[m]()) : 0), s) : (0,require$vega_util.constant)(0.5);
  }
  function interpolate(type, gamma) {
    type = $$1[method(type)];
    return gamma != null && type && type.gamma ? type.gamma(gamma) : type;
  }
  function method(type) {
    return "interpolate" + type.toLowerCase().split("-").map(s => s[0].toUpperCase() + s.slice(1)).join("");
  }
  function colors(palette) {
    if ((0,require$vega_util.isArray)(palette)) {
      return palette;
    }
    const n = palette.length / 6 | 0, c = Array(n);
    for (let i = 0; i < n;) {
      c[i] = "#" + palette.slice(i * 6, ++i * 6);
    }
    return c;
  }
  function apply(_, f) {
    for (const k in _) {
      scheme(k, f(_[k]));
    }
  }
  function scheme(name, scheme) {
    name = name && name.toLowerCase();
    return arguments.length > 1 ? (schemes[name] = scheme, this) : schemes[name];
  }
  function tickCount(scale, count, minStep) {
    let step;
    (0,require$vega_util.isNumber)(count) && (scale.bins && (count = Math.max(count, scale.bins.length)), minStep != null && (count = Math.min(count, Math.floor((0,require$vega_util.span)(scale.domain()) / minStep || 1) + 1)));
    (0,require$vega_util.isObject)(count) && (step = count.step, count = count.interval);
    (0,require$vega_util.isString)(count) && (count = scale.type === "time" ? (0,require$vega_time.timeInterval)(count) : scale.type == "utc" ? (0,require$vega_time.utcInterval)(count) : (0,require$vega_util.error)("Only time and utc scales accept interval strings."), step && (count = count.every(step)));
    return count;
  }
  function validTicks(scale, ticks, count) {
    var range = scale.range();
    let lo = range[0], hi = (0,require$vega_util.peek)(range);
    range = ascending;
    lo > hi && (range = hi, hi = lo, lo = range, range = descending);
    lo = Math.floor(lo);
    hi = Math.ceil(hi);
    ticks = ticks.map(v => [v, scale(v)]).filter(_ => lo <= _[1] && _[1] <= hi).sort(range).map(_ => _[0]);
    if (count > 0 && ticks.length > 1) {
      for (range = [ticks[0], (0,require$vega_util.peek)(ticks)]; ticks.length > count && ticks.length >= 3;) {
        ticks = ticks.filter((_, i) => !(i % 2));
      }
      ticks.length < 3 && (ticks = range);
    }
    return ticks;
  }
  function tickValues(scale, count) {
    return scale.bins ? validTicks(scale, scale.bins, count) : scale.ticks ? scale.ticks(count) : scale.domain();
  }
  function tickFormat(locale, scale, count, specifier, formatType, noSkip) {
    const type = scale.type;
    let format = defaultFormatter;
    if (type === "time" || formatType === "time") {
      format = locale.timeFormat(specifier);
    } else if (type === "utc" || formatType === "utc") {
      format = locale.utcFormat(specifier);
    } else if (isLogarithmic(type)) {
      const varfmt = locale.formatFloat(specifier);
      if (noSkip || scale.bins) {
        format = varfmt;
      } else {
        const test = tickLog(scale, count, !1);
        format = _ => test(_) ? varfmt(_) : "";
      }
    } else {
      scale.tickFormat ? (scale = scale.domain(), format = locale.formatSpan(scale[0], scale[scale.length - 1], count, specifier)) : specifier && (format = locale.format(specifier));
    }
    return format;
  }
  function tickLog(scale, count, values) {
    const ticks = tickValues(scale, count), base = scale.base(), logb = Math.log(base), k = Math.max(1, base * count / ticks.length);
    scale = d => {
      d /= Math.pow(base, Math.round(Math.log(d) / logb));
      d * base < base - 0.5 && (d *= base);
      return d <= k;
    };
    return values ? ticks.filter(scale) : scale;
  }
  function labelValues(scale, count) {
    scale.bins ? (scale = scale.bins, count = scale.slice(0, -1), count.max = (0,require$vega_util.peek)(scale), scale = count) : scale.type === "log" ? scale = tickLog(scale, count, !0) : symbols[scale.type] ? (scale = scale[symbols[scale.type]](), scale = [-Infinity].concat(scale), scale.max = Infinity) : scale = tickValues(scale, count);
    return scale;
  }
  function labelFormat(locale, scale, count, type, specifier, formatType, noSkip) {
    if (formats[scale.type] && formatType !== "time" && formatType !== "utc") {
      count = scale[formats[scale.type]]();
      formatType = count.length;
      noSkip = formatType > 1 ? count[1] - count[0] : count[0];
      let i;
      for (i = 1; i < formatType; ++i) {
        noSkip = Math.min(noSkip, count[i] - count[i - 1]);
      }
      locale = locale.formatSpan(0, noSkip, 30, specifier);
    } else {
      locale = tickFormat(locale, scale, count, specifier, formatType, noSkip);
    }
    return type === "symbol" && (symbols[scale.type] || scale.bins) ? formatRange(locale) : type === "discrete" ? formatDiscrete(locale) : formatPoint(locale);
  }
  function labelFraction(scale) {
    var domain = scale.domain();
    const count = domain.length - 1;
    let lo = +domain[0];
    domain = +(0,require$vega_util.peek)(domain);
    let span = domain - lo;
    scale.type === "threshold" && (scale = count ? span / count : 0.1, lo -= scale, domain += scale, span = domain - lo);
    return value => (value - lo) / span;
  }
  function domainCaption(locale, scale, opt) {
    opt = opt || {};
    var max = Math.max(3, opt.maxlen || 7), specifier = opt.format;
    const type = (opt = opt.formatType) || scale.type;
    (0,require$vega_util.isString)(specifier) && isTemporal(type) && (specifier = specifier.replace(/%a/g, "%A").replace(/%b/g, "%B"));
    locale = specifier || type !== "time" ? specifier || type !== "utc" ? labelFormat(locale, scale, 5, null, specifier, opt, !0) : locale.utcFormat("%A, %d %B %Y, %X UTC") : locale.timeFormat("%A, %d %B %Y, %X");
    if (isDiscretizing(scale.type)) {
      return max = labelValues(scale).slice(1).map(locale), scale = max.length, `${scale} boundar${scale === 1 ? "y" : "ies"}: ${max.join(", ")}`;
    }
    if (isDiscrete(scale.type)) {
      return specifier = scale.domain(), scale = specifier.length, max = scale > max ? specifier.slice(0, max - 2).map(locale).join(", ") + ", ending with " + specifier.slice(-1).map(locale) : specifier.map(locale).join(", "), `${scale} value${scale === 1 ? "" : "s"}: ${max}`;
    }
    max = scale.domain();
    return `values from ${locale(max[0])} to ${locale((0,require$vega_util.peek)(max))}`;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, Band:{enumerable:!0, get:function() {
    return "band";
  }}, BinOrdinal:{enumerable:!0, get:function() {
    return "bin-ordinal";
  }}, DiscreteLegend:{enumerable:!0, get:function() {
    return "discrete";
  }}, Diverging:{enumerable:!0, get:function() {
    return "diverging";
  }}, GradientLegend:{enumerable:!0, get:function() {
    return "gradient";
  }}, Identity:{enumerable:!0, get:function() {
    return "identity";
  }}, Linear:{enumerable:!0, get:function() {
    return "linear";
  }}, Log:{enumerable:!0, get:function() {
    return "log";
  }}, Ordinal:{enumerable:!0, get:function() {
    return "ordinal";
  }}, Point:{enumerable:!0, get:function() {
    return "point";
  }}, Pow:{enumerable:!0, get:function() {
    return "pow";
  }}, Quantile:{enumerable:!0, get:function() {
    return "quantile";
  }}, Quantize:{enumerable:!0, get:function() {
    return "quantize";
  }}, Sequential:{enumerable:!0, get:function() {
    return "sequential";
  }}, Sqrt:{enumerable:!0, get:function() {
    return "sqrt";
  }}, SymbolLegend:{enumerable:!0, get:function() {
    return "symbol";
  }}, Symlog:{enumerable:!0, get:function() {
    return "symlog";
  }}, Threshold:{enumerable:!0, get:function() {
    return "threshold";
  }}, Time:{enumerable:!0, get:function() {
    return "time";
  }}, UTC:{enumerable:!0, get:function() {
    return "utc";
  }}, bandSpace:{enumerable:!0, get:function() {
    return bandSpace;
  }}, domainCaption:{enumerable:!0, get:function() {
    return domainCaption;
  }}, interpolate:{enumerable:!0, get:function() {
    return interpolate;
  }}, interpolateColors:{enumerable:!0, get:function() {
    return interpolateColors;
  }}, interpolateRange:{enumerable:!0, get:function() {
    return interpolateRange;
  }}, isContinuous:{enumerable:!0, get:function() {
    return isContinuous;
  }}, isDiscrete:{enumerable:!0, get:function() {
    return isDiscrete;
  }}, isDiscretizing:{enumerable:!0, get:function() {
    return isDiscretizing;
  }}, isInterpolating:{enumerable:!0, get:function() {
    return isInterpolating;
  }}, isLogarithmic:{enumerable:!0, get:function() {
    return isLogarithmic;
  }}, isQuantile:{enumerable:!0, get:function() {
    return isQuantile;
  }}, isRegisteredScale:{enumerable:!0, get:function() {
    return isRegisteredScale;
  }}, isTemporal:{enumerable:!0, get:function() {
    return isTemporal;
  }}, isValidScaleType:{enumerable:!0, get:function() {
    return isValidScaleType;
  }}, labelFormat:{enumerable:!0, get:function() {
    return labelFormat;
  }}, labelFraction:{enumerable:!0, get:function() {
    return labelFraction;
  }}, labelValues:{enumerable:!0, get:function() {
    return labelValues;
  }}, quantizeInterpolator:{enumerable:!0, get:function() {
    return quantizeInterpolator;
  }}, registerScale:{enumerable:!0, get:function() {
    return registerScale;
  }}, scale:{enumerable:!0, get:function() {
    return scale;
  }}, scaleCopy:{enumerable:!0, get:function() {
    return scaleCopy;
  }}, scaleFraction:{enumerable:!0, get:function() {
    return scaleFraction;
  }}, scaleImplicit:{enumerable:!0, get:function() {
    return require$d3_scale.scaleImplicit;
  }}, scheme:{enumerable:!0, get:function() {
    return scheme;
  }}, tickCount:{enumerable:!0, get:function() {
    return tickCount;
  }}, tickFormat:{enumerable:!0, get:function() {
    return tickFormat;
  }}, tickValues:{enumerable:!0, get:function() {
    return tickValues;
  }}, validTicks:{enumerable:!0, get:function() {
    return validTicks;
  }}});
  var require$vega_util = require("module$node_modules$vega_util$build$index"), require$d3_array = require("module$node_modules$d3_array$src$index");
  module = require("module$node_modules$d3_scale$src$index");
  var require$d3_scale = require("module$node_modules$d3_scale$src$index"), $$1 = require("module$node_modules$d3_interpolate$src$index");
  exports = require("module$node_modules$d3_scale_chromatic$src$index");
  var require$vega_time = require("module$node_modules$vega_time$build$vega_time"), map = Array.prototype.map;
  const slice = Array.prototype.slice, scales = new Map(), VEGA_SCALE = Symbol("vega_scale");
  scale("identity", module.scaleIdentity);
  scale("linear", module.scaleLinear, "continuous");
  scale("log", module.scaleLog, ["continuous", "log"]);
  scale("pow", module.scalePow, "continuous");
  scale("sqrt", module.scaleSqrt, "continuous");
  scale("symlog", module.scaleSymlog, "continuous");
  scale("time", module.scaleTime, ["continuous", "temporal"]);
  scale("utc", module.scaleUtc, ["continuous", "temporal"]);
  scale("sequential", module.scaleSequential, ["continuous", "interpolating"]);
  scale("sequential-linear", module.scaleSequential, ["continuous", "interpolating"]);
  scale("sequential-log", module.scaleSequentialLog, ["continuous", "interpolating", "log"]);
  scale("sequential-pow", module.scaleSequentialPow, ["continuous", "interpolating"]);
  scale("sequential-sqrt", module.scaleSequentialSqrt, ["continuous", "interpolating"]);
  scale("sequential-symlog", module.scaleSequentialSymlog, ["continuous", "interpolating"]);
  scale("diverging-linear", module.scaleDiverging, ["continuous", "interpolating"]);
  scale("diverging-log", module.scaleDivergingLog, ["continuous", "interpolating", "log"]);
  scale("diverging-pow", module.scaleDivergingPow, ["continuous", "interpolating"]);
  scale("diverging-sqrt", module.scaleDivergingSqrt, ["continuous", "interpolating"]);
  scale("diverging-symlog", module.scaleDivergingSymlog, ["continuous", "interpolating"]);
  scale("quantile", module.scaleQuantile, ["discretizing", "quantile"]);
  scale("quantize", module.scaleQuantize, "discretizing");
  scale("threshold", module.scaleThreshold, "discretizing");
  scale("bin-ordinal", scaleBinOrdinal, ["discrete", "discretizing"]);
  scale("ordinal", module.scaleOrdinal, "discrete");
  scale("band", band, "discrete");
  scale("point", function() {
    return pointish(band().paddingInner(1));
  }, "discrete");
  const scaleProps = ["clamp", "base", "constant", "exponent"], schemes = {};
  apply({accent:exports.schemeAccent, category10:exports.schemeCategory10, category20:"1f77b4aec7e8ff7f0effbb782ca02c98df8ad62728ff98969467bdc5b0d58c564bc49c94e377c2f7b6d27f7f7fc7c7c7bcbd22dbdb8d17becf9edae5", category20b:"393b795254a36b6ecf9c9ede6379398ca252b5cf6bcedb9c8c6d31bd9e39e7ba52e7cb94843c39ad494ad6616be7969c7b4173a55194ce6dbdde9ed6", category20c:"3182bd6baed69ecae1c6dbefe6550dfd8d3cfdae6bfdd0a231a35474c476a1d99bc7e9c0756bb19e9ac8bcbddcdadaeb636363969696bdbdbdd9d9d9", dark2:exports.schemeDark2, 
  observable10:exports.schemeObservable10, paired:exports.schemePaired, pastel1:exports.schemePastel1, pastel2:exports.schemePastel2, set1:exports.schemeSet1, set2:exports.schemeSet2, set3:exports.schemeSet3, tableau10:"4c78a8f58518e4575672b7b254a24beeca3bb279a2ff9da69d755dbab0ac", tableau20:"4c78a89ecae9f58518ffbf7954a24b88d27ab79a20f2cf5b43989483bcb6e45756ff9d9879706ebab0acd67195fcbfd2b279a2d6a5c99e765fd8b5a5"}, colors);
  apply({blues:"cfe1f2bed8eca8cee58fc1de74b2d75ba3cf4592c63181bd206fb2125ca40a4a90", greens:"d3eecdc0e6baabdda594d3917bc77d60ba6c46ab5e329a512089430e7735036429", greys:"e2e2e2d4d4d4c4c4c4b1b1b19d9d9d8888887575756262624d4d4d3535351e1e1e", oranges:"fdd8b3fdc998fdb87bfda55efc9244f87f2cf06b18e4580bd14904b93d029f3303", purples:"e2e1efd4d4e8c4c5e0b4b3d6a3a0cc928ec3827cb97566ae684ea25c3696501f8c", reds:"fdc9b4fcb49afc9e80fc8767fa7051f6573fec3f2fdc2a25c81b1db21218970b13", blueGreen:"d5efedc1e8e0a7ddd18bd2be70c6a958ba9144ad77319c5d2089460e7736036429", 
  bluePurple:"ccddecbad0e4a8c2dd9ab0d4919cc98d85be8b6db28a55a6873c99822287730f71", greenBlue:"d3eecec5e8c3b1e1bb9bd8bb82cec269c2ca51b2cd3c9fc7288abd1675b10b60a1", orangeRed:"fddcaffdcf9bfdc18afdad77fb9562f67d53ee6545e24932d32d1ebf130da70403", purpleBlue:"dbdaebc8cee4b1c3de97b7d87bacd15b9fc93a90c01e7fb70b70ab056199045281", purpleBlueGreen:"dbd8eac8cee4b0c3de93b7d872acd1549fc83892bb1c88a3097f8702736b016353", purpleRed:"dcc9e2d3b3d7ce9eccd186c0da6bb2e14da0e23189d91e6fc61159ab07498f023a", redPurple:"fccfccfcbec0faa9b8f98faff571a5ec539ddb3695c41b8aa908808d0179700174", 
  yellowGreen:"e4f4acd1eca0b9e2949ed68880c97c62bb6e47aa5e3297502083440e723b036034", yellowOrangeBrown:"feeaa1fedd84fecc63feb746fca031f68921eb7215db5e0bc54c05ab3d038f3204", yellowOrangeRed:"fee087fed16ffebd59fea849fd903efc7335f9522bee3423de1b20ca0b22af0225", blueOrange:"134b852f78b35da2cb9dcae1d2e5eff2f0ebfce0bafbbf74e8932fc5690d994a07", brownBlueGreen:"704108a0651ac79548e3c78af3e6c6eef1eac9e9e48ed1c74da79e187a72025147", purpleGreen:"5b1667834792a67fb6c9aed3e6d6e8eff0efd9efd5aedda971bb75368e490e5e29", 
  purpleOrange:"4114696647968f83b7b9b4d6dadbebf3eeeafce0bafbbf74e8932fc5690d994a07", redBlue:"8c0d25bf363adf745ef4ae91fbdbc9f2efeed2e5ef9dcae15da2cb2f78b3134b85", redGrey:"8c0d25bf363adf745ef4ae91fcdccbfaf4f1e2e2e2c0c0c0969696646464343434", yellowGreenBlue:"eff9bddbf1b4bde5b594d5b969c5be45b4c22c9ec02182b82163aa23479c1c3185", redYellowBlue:"a50026d4322cf16e43fcac64fedd90faf8c1dcf1ecabd6e875abd04a74b4313695", redYellowGreen:"a50026d4322cf16e43fcac63fedd8df9f7aed7ee8ea4d86e64bc6122964f006837", pinkYellowGreen:"8e0152c0267edd72adf0b3d6faddedf5f3efe1f2cab6de8780bb474f9125276419", 
  spectral:"9e0142d13c4bf0704afcac63fedd8dfbf8b0e0f3a1a9dda269bda94288b55e4fa2", viridis:"440154470e61481a6c482575472f7d443a834144873d4e8a39568c35608d31688e2d708e2a788e27818e23888e21918d1f988b1fa08822a8842ab07f35b77943bf7154c56866cc5d7ad1518fd744a5db36bcdf27d2e21be9e51afde725", magma:"0000040404130b0924150e3720114b2c11603b0f704a107957157e651a80721f817f24828c29819a2e80a8327db6377ac43c75d1426fde4968e95462f1605df76f5cfa7f5efc8f65fe9f6dfeaf78febf84fece91fddea0fcedaffcfdbf", inferno:"0000040403130c0826170c3b240c4f330a5f420a68500d6c5d126e6b176e781c6d86216b932667a12b62ae305cbb3755c73e4cd24644dd513ae65c30ed6925f3771af8850ffb9506fca50afcb519fac62df6d645f2e661f3f484fcffa4", 
  plasma:"0d088723069033059742039d5002a25d01a66a00a87801a88405a7900da49c179ea72198b12a90ba3488c33d80cb4779d35171da5a69e16462e76e5bed7953f2834cf68f44fa9a3dfca636fdb32ffec029fcce25f9dc24f5ea27f0f921", cividis:"", 
  rainbow:"6e40aa883eb1a43db3bf3cafd83fa4ee4395fe4b83ff576eff6659ff7847ff8c38f3a130e2b72fcfcc36bee044aff05b8ff4576ff65b52f6673af27828ea8d1ddfa319d0b81cbecb23abd82f96e03d82e14c6edb5a5dd0664dbf6e40aa", sinebow:"ff4040fc582af47218e78d0bd5a703bfbf00a7d5038de70b72f41858fc2a40ff402afc5818f4720be78d03d5a700bfbf03a7d50b8de71872f42a58fc4040ff582afc7218f48d0be7a703d5bf00bfd503a7e70b8df41872fc2a58ff4040", turbo:"", 
  browns:"eedbbdecca96e9b97ae4a865dc9856d18954c7784cc0673fb85536ad44339f3632", tealBlues:"bce4d89dd3d181c3cb65b3c245a2b9368fae347da0306a932c5985", teals:"bbdfdfa2d4d58ac9c975bcbb61b0af4da5a43799982b8b8c1e7f7f127273006667", warmGreys:"dcd4d0cec5c1c0b8b4b3aaa7a59c9998908c8b827f7e7673726866665c5a59504e", goldGreen:"f4d166d5ca60b6c35c98bb597cb25760a6564b9c533f8f4f33834a257740146c36", goldOrange:"f4d166f8be5cf8aa4cf5983bf3852aef701be2621fd65322c54923b142239e3a26", goldRed:"f4d166f6be59f9aa51fc964ef6834bee734ae56249db5247cf4244c43141b71d3e", 
  lightGreyRed:"efe9e6e1dad7d5cbc8c8bdb9bbaea9cd967ddc7b43e15f19df4011dc000b", lightGreyTeal:"e4eaead6dcddc8ced2b7c2c7a6b4bc64b0bf22a6c32295c11f85be1876bc", lightMulti:"e0f1f2c4e9d0b0de9fd0e181f6e072f6c053f3993ef77440ef4a3c", lightOrange:"f2e7daf7d5baf9c499fab184fa9c73f68967ef7860e8645bde515bd43d5b", lightTealBlue:"e3e9e0c0dccf9aceca7abfc859afc0389fb9328dad2f7ca0276b95255988", darkBlue:"3232322d46681a5c930074af008cbf05a7ce25c0dd38daed50f3faffffff", darkGold:"3c3c3c584b37725e348c7631ae8b2bcfa424ecc31ef9de30fff184ffffff", 
  darkGreen:"3a3a3a215748006f4d048942489e4276b340a6c63dd2d836ffeb2cffffaa", darkMulti:"3737371f5287197d8c29a86995ce3fffe800ffffff", darkRed:"3434347036339e3c38cc4037e75d1eec8620eeab29f0ce32ffeb2c"}, _ => interpolateColors(colors(_)));
  const defaultFormatter = value => (0,require$vega_util.isArray)(value) ? value.map(v => String(v)) : String(value), ascending = (a, b) => a[1] - b[1], descending = (a, b) => b[1] - a[1], symbols = {quantile:"quantiles", quantize:"thresholds", threshold:"domain"}, formats = {quantile:"quantiles", quantize:"domain"}, formatRange = format => (value, index, array) => {
    index = array[index + 1];
    array = array.max;
    array = array != null ? array : Infinity;
    index = index != null ? index : array;
    value = Number.isFinite(value) ? format(value) : null;
    index = Number.isFinite(index) ? format(index) : null;
    return value && index ? value + " – " + index : index ? "\x3c " + index : "≥ " + value;
  }, formatDiscrete = format => (value, index) => index ? format(value) : null, formatPoint = format => value => format(value);
};

//# sourceMappingURL=module$node_modules$vega_scale$build$vega_scale.js.map
