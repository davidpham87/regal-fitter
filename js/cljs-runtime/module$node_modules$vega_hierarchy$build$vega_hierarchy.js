shadow$provide.module$node_modules$vega_hierarchy$build$vega_hierarchy = function(require, module, exports) {
  function lookup(tree, key, filter) {
    const map = {};
    tree.each(node => {
      const t = node.data;
      filter(t) && (map[key(t)] = node);
    });
    tree.lookup = map;
    return tree;
  }
  function Nest(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function nest() {
    function apply(array, depth) {
      if (depth >= keys.length) {
        return array;
      }
      const n = array.length, key = keys[depth++], valuesByKey = {}, result = {};
      let i = -1, keyValue, value, values;
      for (; ++i < n;) {
        keyValue = key(value = array[i]) + "", (values = valuesByKey[keyValue]) ? values.push(value) : valuesByKey[keyValue] = [value];
      }
      for (keyValue in valuesByKey) {
        result[keyValue] = apply(valuesByKey[keyValue], depth);
      }
      return result;
    }
    function entries(map, depth) {
      if (++depth > keys.length) {
        return map;
      }
      const array = [];
      for (const key in map) {
        array.push({key, values:entries(map[key], depth)});
      }
      return array;
    }
    const keys = [], nest = {entries:array => entries(apply(array, 0), 0), key:d => (keys.push(d), nest)};
    return nest;
  }
  function HierarchyLayout(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function setParams(layout, params, _) {
    for (let p, i = 0, n = params.length; i < n; ++i) {
      if (p = params[i], p in _) {
        layout[p](_[p]);
      }
    }
  }
  function Pack(params) {
    HierarchyLayout.call(this, params);
  }
  function Partition(params) {
    HierarchyLayout.call(this, params);
  }
  function Stratify(params) {
    require$vega_dataflow.Transform.call(this, null, params);
  }
  function Tree(params) {
    HierarchyLayout.call(this, params);
  }
  function TreeLinks(params) {
    require$vega_dataflow.Transform.call(this, [], params);
  }
  function Treemap(params) {
    HierarchyLayout.call(this, params);
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, nest:{enumerable:!0, get:function() {
    return Nest;
  }}, pack:{enumerable:!0, get:function() {
    return Pack;
  }}, partition:{enumerable:!0, get:function() {
    return Partition;
  }}, stratify:{enumerable:!0, get:function() {
    return Stratify;
  }}, tree:{enumerable:!0, get:function() {
    return Tree;
  }}, treelinks:{enumerable:!0, get:function() {
    return TreeLinks;
  }}, treemap:{enumerable:!0, get:function() {
    return Treemap;
  }}});
  var require$vega_dataflow = require("module$node_modules$vega_dataflow$build$vega_dataflow"), require$vega_util = require("module$node_modules$vega_util$build$index"), require$d3_hierarchy = require("module$node_modules$d3_hierarchy$src$index");
  Nest.Definition = {type:"Nest", metadata:{treesource:!0, changes:!0}, params:[{name:"keys", type:"field", array:!0}, {name:"generate", type:"boolean"}]};
  const children = n => n.values;
  (0,require$vega_util.inherits)(Nest, require$vega_dataflow.Transform, {transform(_, pulse) {
    pulse.source || (0,require$vega_util.error)("Nest transform requires an upstream data source.");
    var gen = _.generate, mod = _.modified(), out = pulse.clone(), tree = this.value;
    if (!tree || mod || pulse.changed()) {
      tree && tree.each(node => {
        node.children && (0,require$vega_dataflow.isTuple)(node.data) && out.rem.push(node.data);
      }), this.value = tree = (0,require$d3_hierarchy.hierarchy)({values:(0,require$vega_util.array)(_.keys).reduce((n, k) => {
        n.key(k);
        return n;
      }, nest()).entries(out.source)}, children), gen && tree.each(node => {
        node.children && (node = (0,require$vega_dataflow.ingest)(node.data), out.add.push(node), out.source.push(node));
      }), lookup(tree, require$vega_dataflow.tupleid, require$vega_dataflow.tupleid);
    }
    out.source.root = tree;
    return out;
  }});
  const defaultSeparation = (a, b) => a.parent === b.parent ? 1 : 2;
  (0,require$vega_util.inherits)(HierarchyLayout, require$vega_dataflow.Transform, {transform(_, pulse) {
    pulse.source && pulse.source.root || (0,require$vega_util.error)(this.constructor.name + " transform requires a backing tree data source.");
    const layout = this.layout(_.method), fields = this.fields, root = pulse.source.root, as = _.as || fields;
    _.field ? root.sum(_.field) : root.count();
    _.sort && root.sort((0,require$vega_dataflow.stableCompare)(_.sort, d => d.data));
    setParams(layout, this.params, _);
    layout.separation && layout.separation(_.separation !== !1 ? defaultSeparation : require$vega_util.one);
    try {
      this.value = layout(root);
    } catch (err) {
      (0,require$vega_util.error)(err);
    }
    root.each(node => {
      const t = node.data, n = fields.length - 1;
      for (let i = 0; i < n; ++i) {
        t[as[i]] = node[fields[i]];
      }
      t[as[n]] = node.children ? node.children.length : 0;
    });
    return pulse.reflow(_.modified()).modifies(as).modifies("leaf");
  }});
  require = ["x", "y", "r", "depth", "children"];
  Pack.Definition = {type:"Pack", metadata:{tree:!0, modifies:!0}, params:[{name:"field", type:"field"}, {name:"sort", type:"compare"}, {name:"padding", type:"number", "default":0}, {name:"radius", type:"field", "default":null}, {name:"size", type:"number", array:!0, length:2}, {name:"as", type:"string", array:!0, length:require.length, "default":require}]};
  (0,require$vega_util.inherits)(Pack, HierarchyLayout, {layout:require$d3_hierarchy.pack, params:["radius", "size", "padding"], fields:require});
  require = "x0 y0 x1 y1 depth children".split(" ");
  Partition.Definition = {type:"Partition", metadata:{tree:!0, modifies:!0}, params:[{name:"field", type:"field"}, {name:"sort", type:"compare"}, {name:"padding", type:"number", "default":0}, {name:"round", type:"boolean", "default":!1}, {name:"size", type:"number", array:!0, length:2}, {name:"as", type:"string", array:!0, length:require.length, "default":require}]};
  (0,require$vega_util.inherits)(Partition, HierarchyLayout, {layout:require$d3_hierarchy.partition, params:["size", "round", "padding"], fields:require});
  Stratify.Definition = {type:"Stratify", metadata:{treesource:!0}, params:[{name:"key", type:"field", required:!0}, {name:"parentKey", type:"field", required:!0}]};
  (0,require$vega_util.inherits)(Stratify, require$vega_dataflow.Transform, {transform(_, pulse) {
    pulse.source || (0,require$vega_util.error)("Stratify transform requires an upstream data source.");
    let tree = this.value;
    const mod = _.modified(), out = pulse.fork(pulse.ALL).materialize(pulse.SOURCE);
    pulse = !tree || mod || pulse.changed(pulse.ADD_REM) || pulse.modified(_.key.fields) || pulse.modified(_.parentKey.fields);
    out.source = out.source.slice();
    pulse && (tree = out.source.length ? lookup((0,require$d3_hierarchy.stratify)().id(_.key).parentId(_.parentKey)(out.source), _.key, require$vega_util.truthy) : lookup((0,require$d3_hierarchy.stratify)()([{}]), _.key, _.key));
    out.source.root = this.value = tree;
    return out;
  }});
  const Layouts = {tidy:require$d3_hierarchy.tree, cluster:require$d3_hierarchy.cluster};
  require = ["x", "y", "depth", "children"];
  Tree.Definition = {type:"Tree", metadata:{tree:!0, modifies:!0}, params:[{name:"field", type:"field"}, {name:"sort", type:"compare"}, {name:"method", type:"enum", "default":"tidy", values:["tidy", "cluster"]}, {name:"size", type:"number", array:!0, length:2}, {name:"nodeSize", type:"number", array:!0, length:2}, {name:"separation", type:"boolean", "default":!0}, {name:"as", type:"string", array:!0, length:require.length, "default":require}]};
  (0,require$vega_util.inherits)(Tree, HierarchyLayout, {layout(method) {
    method = method || "tidy";
    if ((0,require$vega_util.hasOwnProperty)(Layouts, method)) {
      return Layouts[method]();
    }
    (0,require$vega_util.error)("Unrecognized Tree layout method: " + method);
  }, params:["size", "nodeSize"], fields:require});
  TreeLinks.Definition = {type:"TreeLinks", metadata:{tree:!0, generates:!0, changes:!0}, params:[]};
  (0,require$vega_util.inherits)(TreeLinks, require$vega_dataflow.Transform, {transform(_, pulse) {
    _ = this.value;
    const tree = pulse.source && pulse.source.root, out = pulse.fork(pulse.NO_SOURCE), lut = {};
    tree || (0,require$vega_util.error)("TreeLinks transform requires a tree data source.");
    pulse.changed(pulse.ADD_REM) ? (out.rem = _, pulse.visit(pulse.SOURCE, t => lut[(0,require$vega_dataflow.tupleid)(t)] = 1), tree.each(node => {
      const t = node.data;
      (node = node.parent && node.parent.data) && lut[(0,require$vega_dataflow.tupleid)(t)] && lut[(0,require$vega_dataflow.tupleid)(node)] && out.add.push((0,require$vega_dataflow.ingest)({source:node, target:t}));
    }), this.value = out.add) : pulse.changed(pulse.MOD) && (pulse.visit(pulse.MOD, t => lut[(0,require$vega_dataflow.tupleid)(t)] = 1), _.forEach(link => {
      (lut[(0,require$vega_dataflow.tupleid)(link.source)] || lut[(0,require$vega_dataflow.tupleid)(link.target)]) && out.mod.push(link);
    }));
    return out;
  }});
  const Tiles = {binary:require$d3_hierarchy.treemapBinary, dice:require$d3_hierarchy.treemapDice, slice:require$d3_hierarchy.treemapSlice, slicedice:require$d3_hierarchy.treemapSliceDice, squarify:require$d3_hierarchy.treemapSquarify, resquarify:require$d3_hierarchy.treemapResquarify};
  require = "x0 y0 x1 y1 depth children".split(" ");
  Treemap.Definition = {type:"Treemap", metadata:{tree:!0, modifies:!0}, params:[{name:"field", type:"field"}, {name:"sort", type:"compare"}, {name:"method", type:"enum", "default":"squarify", values:"squarify resquarify binary dice slice slicedice".split(" ")}, {name:"padding", type:"number", "default":0}, {name:"paddingInner", type:"number", "default":0}, {name:"paddingOuter", type:"number", "default":0}, {name:"paddingTop", type:"number", "default":0}, {name:"paddingRight", type:"number", "default":0}, 
  {name:"paddingBottom", type:"number", "default":0}, {name:"paddingLeft", type:"number", "default":0}, {name:"ratio", type:"number", "default":1.618033988749895}, {name:"round", type:"boolean", "default":!1}, {name:"size", type:"number", array:!0, length:2}, {name:"as", type:"string", array:!0, length:require.length, "default":require}]};
  (0,require$vega_util.inherits)(Treemap, HierarchyLayout, {layout() {
    const x = (0,require$d3_hierarchy.treemap)();
    x.ratio = _ => {
      const t = x.tile();
      t.ratio && x.tile(t.ratio(_));
    };
    x.method = _ => {
      (0,require$vega_util.hasOwnProperty)(Tiles, _) ? x.tile(Tiles[_]) : (0,require$vega_util.error)("Unrecognized Treemap layout method: " + _);
    };
    return x;
  }, params:"method ratio size round padding paddingInner paddingOuter paddingTop paddingRight paddingBottom paddingLeft".split(" "), fields:require});
};

//# sourceMappingURL=module$node_modules$vega_hierarchy$build$vega_hierarchy.js.map
