shadow$provide.module$node_modules$vega_interpreter$build$vega_interpreter = function(require, module, exports) {
  function interpret(ast, fn, params, datum, event, item) {
    const $ = n => Visitors[n.type]($, n);
    $.memberDepth = 0;
    $.fn = Object.create(fn);
    $.params = params;
    $.datum = datum;
    $.event = event;
    $.item = item;
    EventFunctions.forEach(f => $.fn[f] = (...args) => event.vega[f](...args));
    return $(ast);
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, expressionInterpreter:{enumerable:!0, get:function() {
    return expression;
  }}});
  var require$vega_util = require("module$node_modules$vega_util$build$index"), Constants = {NaN, E:Math.E, LN2:Math.LN2, LN10:Math.LN10, LOG2E:Math.LOG2E, LOG10E:Math.LOG10E, PI:Math.PI, SQRT1_2:Math.SQRT1_2, SQRT2:Math.SQRT2, MIN_VALUE:Number.MIN_VALUE, MAX_VALUE:Number.MAX_VALUE}, Ops = {"*":(a, b) => a * b, "+":(a, b) => a + b, "-":(a, b) => a - b, "/":(a, b) => a / b, "%":(a, b) => a % b, "\x3e":(a, b) => a > b, "\x3c":(a, b) => a < b, "\x3c\x3d":(a, b) => a <= b, "\x3e\x3d":(a, b) => a >= b, 
  "\x3d\x3d":(a, b) => a == b, "!\x3d":(a, b) => a != b, "\x3d\x3d\x3d":(a, b) => a === b, "!\x3d\x3d":(a, b) => a !== b, "\x26":(a, b) => a & b, "|":(a, b) => a | b, "^":(a, b) => a ^ b, "\x3c\x3c":(a, b) => a << b, "\x3e\x3e":(a, b) => a >> b, "\x3e\x3e\x3e":(a, b) => a >>> b}, Unary = {"+":a => +a, "-":a => -a, "~":a => ~a, "!":a => !a};
  const slice = Array.prototype.slice, apply = (m, args, cast) => {
    cast = cast ? cast(args[0]) : args[0];
    return cast[m].apply(cast, slice.call(args, 1));
  };
  var Functions = {isNaN:Number.isNaN, isFinite:Number.isFinite, abs:Math.abs, acos:Math.acos, asin:Math.asin, atan:Math.atan, atan2:Math.atan2, ceil:Math.ceil, cos:Math.cos, exp:Math.exp, floor:Math.floor, log:Math.log, max:Math.max, min:Math.min, pow:Math.pow, random:Math.random, round:Math.round, sin:Math.sin, sqrt:Math.sqrt, tan:Math.tan, clamp:(a, b, c) => Math.max(b, Math.min(c, a)), now:Date.now, utc:Date.UTC, datetime:(yearOrTimestring, m = 0, d = 1, H = 0, M = 0, S = 0, ms = 0) => (0,require$vega_util.isString)(yearOrTimestring) ? 
  new Date(yearOrTimestring) : new Date(yearOrTimestring, m, d, H, M, S, ms), date:d => (new Date(d)).getDate(), day:d => (new Date(d)).getDay(), year:d => (new Date(d)).getFullYear(), month:d => (new Date(d)).getMonth(), hours:d => (new Date(d)).getHours(), minutes:d => (new Date(d)).getMinutes(), seconds:d => (new Date(d)).getSeconds(), milliseconds:d => (new Date(d)).getMilliseconds(), time:d => (new Date(d)).getTime(), timezoneoffset:d => (new Date(d)).getTimezoneOffset(), utcdate:d => (new Date(d)).getUTCDate(), 
  utcday:d => (new Date(d)).getUTCDay(), utcyear:d => (new Date(d)).getUTCFullYear(), utcmonth:d => (new Date(d)).getUTCMonth(), utchours:d => (new Date(d)).getUTCHours(), utcminutes:d => (new Date(d)).getUTCMinutes(), utcseconds:d => (new Date(d)).getUTCSeconds(), utcmilliseconds:d => (new Date(d)).getUTCMilliseconds(), length:x => x.length, join:function() {
    return apply("join", arguments);
  }, indexof:function() {
    return apply("indexOf", arguments);
  }, lastindexof:function() {
    return apply("lastIndexOf", arguments);
  }, slice:function() {
    return apply("slice", arguments);
  }, reverse:x => x.slice().reverse(), sort:x => x.slice().sort(require$vega_util.ascending), parseFloat, parseInt, upper:x => String(x).toUpperCase(), lower:x => String(x).toLowerCase(), substring:function() {
    return apply("substring", arguments, String);
  }, split:function() {
    return apply("split", arguments, String);
  }, replace:function() {
    return apply("replace", arguments, String);
  }, trim:x => String(x).trim(), btoa:x => btoa(x), atob:x => atob(x), regexp:RegExp, test:(r, t) => RegExp(r).test(t)};
  const EventFunctions = "view item group xy x y".split(" "), DisallowedMethods = new Set([Function, eval, setTimeout, setInterval]);
  typeof setImmediate === "function" && DisallowedMethods.add(setImmediate);
  const Visitors = {Literal:($, n) => n.value, Identifier:($, n) => {
    n = n.name;
    return $.memberDepth > 0 ? n : n === "datum" ? $.datum : n === "event" ? $.event : n === "item" ? $.item : Constants[n] || $.params["$" + n];
  }, MemberExpression:($, n) => {
    const d = !n.computed, o = $(n.object);
    d && ($.memberDepth += 1);
    n = $(n.property);
    d && --$.memberDepth;
    if (DisallowedMethods.has(o[n])) {
      console.error(`Prevented interpretation of member "${n}" which could lead to insecure code execution`);
    } else {
      return o[n];
    }
  }, CallExpression:($, n) => {
    const args = n.arguments;
    n = n.callee.name;
    n.startsWith("_") && (n = n.slice(1));
    return n === "if" ? $(args[0]) ? $(args[1]) : $(args[2]) : ($.fn[n] || Functions[n]).apply($.fn, args.map($));
  }, ArrayExpression:($, n) => n.elements.map($), BinaryExpression:($, n) => Ops[n.operator]($(n.left), $(n.right)), UnaryExpression:($, n) => Unary[n.operator]($(n.argument)), ConditionalExpression:($, n) => $(n.test) ? $(n.consequent) : $(n.alternate), LogicalExpression:($, n) => n.operator === "\x26\x26" ? $(n.left) && $(n.right) : $(n.left) || $(n.right), ObjectExpression:($, n) => n.properties.reduce((o, p) => {
    $.memberDepth += 1;
    const k = $(p.key);
    --$.memberDepth;
    p = $(p.value);
    require$vega_util.DisallowedObjectProperties.has(k) ? console.error(`Prevented interpretation of property "${k}" which could lead to insecure code execution`) : DisallowedMethods.has(p) ? console.error(`Prevented interpretation of method "${k}" which could lead to insecure code execution`) : o[k] = p;
    return o;
  }, {})};
  var expression = {operator(ctx, expr) {
    const ast = expr.ast, fn = ctx.functions;
    return _ => interpret(ast, fn, _);
  }, parameter(ctx, expr) {
    const ast = expr.ast, fn = ctx.functions;
    return (datum, _) => interpret(ast, fn, _, datum);
  }, event(ctx, expr) {
    const ast = expr.ast, fn = ctx.functions;
    return event => interpret(ast, fn, void 0, void 0, event);
  }, handler(ctx, expr) {
    const ast = expr.ast, fn = ctx.functions;
    return (_, event) => interpret(ast, fn, _, event.item && event.item.datum, event);
  }, encode(ctx, encode) {
    const {marktype, channels} = encode, fn = ctx.functions, swap = marktype === "group" || marktype === "image" || marktype === "rect";
    return (item, _) => {
      const datum = item.datum;
      let m = 0, v;
      for (const name in channels) {
        v = interpret(channels[name].ast, fn, _, datum, void 0, item), item[name] !== v && (item[name] = v, m = 1);
      }
      marktype !== "rule" && (channels.x2 && (channels.x ? (swap && item.x > item.x2 && (_ = item.x, item.x = item.x2, item.x2 = _), item.width = item.x2 - item.x) : item.x = item.x2 - (item.width || 0)), channels.xc && (item.x = item.xc - (item.width || 0) / 2), channels.y2 && (channels.y ? (swap && item.y > item.y2 && (_ = item.y, item.y = item.y2, item.y2 = _), item.height = item.y2 - item.y) : item.y = item.y2 - (item.height || 0)), channels.yc && (item.y = item.yc - (item.height || 0) / 2));
      return m;
    };
  }};
};

//# sourceMappingURL=module$node_modules$vega_interpreter$build$vega_interpreter.js.map
