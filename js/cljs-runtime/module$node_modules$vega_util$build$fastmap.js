shadow$provide.module$node_modules$vega_util$build$fastmap = function(require, module, exports) {
  function fastmap(input) {
    function has(key) {
      return (0,default$$require$_DOT__SLASH_hasOwnProperty_DOT_js.default)(obj, key) && obj[key] !== NULL;
    }
    let obj = {}, test;
    const map = {size:0, empty:0, object:obj, has, get(key) {
      return has(key) ? obj[key] : void 0;
    }, set(key, value) {
      has(key) || (++map.size, obj[key] === NULL && --map.empty);
      obj[key] = value;
      return this;
    }, delete(key) {
      has(key) && (--map.size, ++map.empty, obj[key] = NULL);
      return this;
    }, clear() {
      map.size = map.empty = 0;
      map.object = obj = {};
    }, test(fn) {
      return arguments.length ? (test = fn, map) : test;
    }, clean() {
      const next = {};
      let size = 0;
      for (const key in obj) {
        const value = obj[key];
        value === NULL || test && test(value) || (next[key] = value, ++size);
      }
      map.size = size;
      map.empty = 0;
      map.object = obj = next;
    }};
    input && Object.keys(input).forEach(key => {
      map.set(key, input[key]);
    });
    return map;
  }
  Object.defineProperties(exports, {__esModule:{enumerable:!0, value:!0}, default:{enumerable:!0, get:function() {
    return fastmap;
  }}});
  module = require("module$node_modules$vega_util$build$hasOwnProperty");
  var default$$require$_DOT__SLASH_hasOwnProperty_DOT_js = require.esmDefault(module);
  const NULL = {};
};

//# sourceMappingURL=module$node_modules$vega_util$build$fastmap.js.map
